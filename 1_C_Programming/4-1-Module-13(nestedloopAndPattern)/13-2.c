#include <stdio.h>   // ইনপুট-আউটপুটের জন্য হেডার ফাইল

int main() { 
    int n;                 // ইউজার ইনপুট রাখার জন্য ভ্যারিয়েবল n
    scanf("%d", &n);       // ইউজার থেকে সংখ্যা ইনপুট নিচ্ছি

    int star = n;          // প্রথম লাইনে n সংখ্যক * প্রিন্ট হবে

    for (int i = 1; i <= n; i++) {          // n বার লুপ চলবে (n লাইনের জন্য)
        for (int j = 1; j <= star; j++) {   // প্রতিটি লাইনে star সংখ্যক * প্রিন্ট হবে
            printf("*");                   // * প্রিন্ট করলাম
        }
        printf("\n");       // এক লাইন শেষ, পরের লাইনে যাচ্ছি
        star--;             // পরের লাইনে তারকার সংখ্যা ১ কমিয়ে দিচ্ছি
    }

    return 0;   // প্রোগ্রাম সফলভাবে শেষ ✅
}

/*
📥 Input:
5

📤 Output:
*****
****
***
**
*

🔍 Short Note:
এই প্রোগ্রামটি একটি উল্টো বাম-কোণ ভিত্তিক (left-aligned) তারকা প্যাটার্ন প্রিন্ট করে 🌠
প্রথম লাইনে `n` সংখ্যক তারকা ছাপায়, তারপর একে একে তারকার সংখ্যা ১ করে কমিয়ে দেয়।

ভেতরের লুপ তারকা ছাপে এবং বাইরের লুপ লাইনের জন্য কাজ করে।
প্রতিবার `star--` করার কারণে প্রতিটি পরবর্তী লাইনে একটি কম তারকা প্রিন্ট হয়। 😊
*/
