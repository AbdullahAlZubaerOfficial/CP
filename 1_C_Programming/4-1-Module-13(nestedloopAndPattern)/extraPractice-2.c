#include <stdio.h>

int main() { 
   int n;
   scanf("%d", &n);  // ইউজার থেকে কত লাইন হবে তা ইনপুট নিচ্ছে
   
   int star = n;     // প্রথম লাইনে n টা তারকা থাকবে
   int space = 0;    // প্রথম লাইনে কোনো স্পেস থাকবে না

   for(int i = 1; i <= n; i++){  // n বার লুপ চলবে প্রতিটি লাইনের জন্য
       
       for(int j = 1; j <= space; j++){
           printf(" ");  // প্রতিটি লাইনের শুরুতে নির্দিষ্ট সংখ্যক স্পেস প্রিন্ট হবে
       }

       for(int j = 1; j <= star; j++){
           printf("*");  // স্পেসের পরে তারকা প্রিন্ট হবে
       }

       printf("\n");  // এক লাইন প্রিন্ট শেষ হলে নতুন লাইনে যাবে

       space++;  // পরবর্তী লাইনে স্পেস ১টা করে বাড়বে
       star--;   // এবং তারকার সংখ্যা ১টা করে কমবে
   }

   return 0;
}

/*
📥 Input:
4

📤 Output:
****
 ***
  **
   *

📝 Short Note:
এই প্রোগ্রামটি এমন একটি pattern প্রিন্ট করে যেটি ডান দিকে হেলানো উল্টো ত্রিভুজের মতো দেখতে।
প্রথম লাইনে n সংখ্যক তারকা থাকে, এবং প্রতিটি পরবর্তী লাইনে একটি করে তারকা কমে ও একটি করে স্পেস বাড়ে।

🔹 Time Complexity: O(n^2)
🔹 Space Complexity: O(1)

যাদের pattern programming শুরু করার ইচ্ছা আছে, তাদের জন্য এটা চমৎকার একটি উদাহরণ! 😊
*/
