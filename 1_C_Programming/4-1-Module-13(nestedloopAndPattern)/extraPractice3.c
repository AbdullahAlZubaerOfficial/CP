#include <stdio.h>

int main() { 
    int n;
    scanf("%d", &n);  // ইউজার থেকে ইনপুট নিচ্ছে - মোট কত লাইনের pattern হবে

    int star = 2 * n - 1;  // প্রথম লাইনে কতগুলো তারকা থাকবে (সবচেয়ে বড় সংখ্যা)
    int space = 0;         // প্রথম লাইনে কোন স্পেস থাকবে না

    for(int i = 1; i <= n; i++) {
        // প্রতি লাইনের শুরুতে কতগুলো স্পেস প্রিন্ট হবে
        for(int j = 1; j <= space; j++) {
            printf(" ");
        }

        // প্রতি লাইনে কতগুলো তারকা প্রিন্ট হবে
        for(int j = 1; j <= star; j++) {
            printf("*");
        }

        printf("\n");  // লাইন শেষে নতুন লাইনে যাওয়া

        star = star - 2;  // তারকার সংখ্যা প্রতি লাইনে ২টি করে কমছে
        space++;          // স্পেস প্রতি লাইনে ১টি করে বাড়ছে
    }

    return 0;
}


/*
📥 Input:
5

📤 Output:
*********
 *******
  *****
   ***
    *

📝 Short Note:
🔹 এই কোডটি একটি উল্টো পিরামিড (inverted pyramid) pattern প্রিন্ট করে।
🔹 প্রথম লাইনে সর্বোচ্চ সংখ্যক তারকা থাকে (2*n - 1), তারপর প্রতি লাইনে ২টি করে কমে।
🔹 প্রতিটি লাইনের শুরুতে space প্রিন্ট করে তারপরে তারকা প্রিন্ট হয় যাতে সেন্টার এলাইন থাকে।

📌 Time Complexity: O(n^2)
📌 Space Complexity: O(1)

✅ এই ধরণের pattern প্র্যাকটিস করলে loop ও nested loop ভালোভাবে বোঝা যায়! 💪

*/
