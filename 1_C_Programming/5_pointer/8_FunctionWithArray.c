#include <stdio.h> // স্ট্যান্ডার্ড ইনপুট আউটপুট হেডার ফাইল

// একটি ফাংশন, যেটি একটি ইন্টিজার অ্যারে ইনপুট হিসেবে নেয়
void fun(int a[]) {
    a[1] = 200; // অ্যারেটির ১ নম্বর ইনডেক্সের মান ২০০ তে পরিবর্তন করা হচ্ছে
}

int main() { 
     
    int a[5] = {10, 20, 30, 40, 50}; // একটি অ্যারে ডিক্লেয়ার করা হয়েছে যেটির মধ্যে ৫টি মান আছে

    fun(a); // অ্যারেটি fun() ফাংশনে পাঠানো হচ্ছে

    for(int i = 0; i < 5; i++) { // ০ থেকে ৪ পর্যন্ত লুপ চালিয়ে
        printf("%d ", a[i]);    // অ্যারেটির প্রতিটি মান প্রিন্ট করা হচ্ছে
    }
   
    return 0; // প্রোগ্রাম সফলভাবে শেষ করার জন্য 0 রিটার্ন করা হচ্ছে
}


/* 
📥 Input:
[কোনো ইনপুট লাগে না কারণ অ্যারে আগে থেকেই ডিফাইন করা আছে]

📤 Output:
10 200 30 40 50

📝 Short Note:
- C ভাষায় যখন একটি অ্যারে ফাংশনে পাঠানো হয়, তখন এটি **reference** হিসেবে পাঠানো হয়।
- তাই `fun()` ফাংশনে অ্যারেটির যেকোনো ইলিমেন্ট পরিবর্তন করলে, তা মূল অ্যারেতেও প্রতিফলিত হয়।
- এখানে `a[1] = 20` ছিল, কিন্তু `fun()` ফাংশনের মাধ্যমে `a[1] = 200` করে দেওয়া হয়েছে।
*/
