#include <stdio.h>  // 🌟 Input-output function-এর হেডার

// 📤 Recursive ফাংশন: সংখ্যা ভেঙে একে একে digit প্রিন্ট করবে
void printDigit(int n)
{
    if (n == 0)
    {
        return; // ❌ Base case: 0 হলে আর কিছু করার নেই
    }

    printDigit(n / 10); // 🔁 আগে আগের digit গুলো প্রিন্ট কর

    int lastDigit = n % 10; // 🔢 শেষ digit বের কর
    printf("%d ", lastDigit); // 📤 প্রিন্ট কর
}

int main()
{
    int t;                  // 🔁 টেস্ট কেস সংখ্যা
    scanf("%d", &t);        // 📥 টেস্ট কেস ইনপুট

    while (t--)            // 🔁 প্রতিটি টেস্ট কেসের জন্য
    {
        int n;
        scanf("%d", &n);    // 📥 প্রতিটি সংখ্যার ইনপুট

        printDigit(n);      // 🔄 রিকার্সিভলি প্রতিটি digit প্রিন্ট

        if (n == 0)         // ⚠️ যদি ইনপুট 0 হয়, উপরের ফাংশন কিছু করবে না
        {
            printf("0");   // ➤ তাই আলাদা করে 0 প্রিন্ট করতে হবে
        }

        printf("\n");       // ⬇️ প্রতিটি টেস্ট কেস শেষে নিউ লাইন
    }

    return 0;               // ✅ প্রোগ্রাম শেষ
}


/*
==========================
📥 Input:
3
121
39
123456

📤 Output:
1 2 1
3 9
1 2 3 4 5 6

==========================
🧠 Short Note:
- `printDigit()` ফাংশন রিকার্সিভভাবে কাজ করে।
  ➤ আগে সংখ্যা ভাগ করে ছোট করে নেয় (n/10)
  ➤ পরে ভাগশেষ (n % 10) দিয়ে একে একে digit গুলো প্রিন্ট করে
  ➤ এইভাবে সংখ্যা বাম দিক থেকে ডান দিকে ভেঙে প্রিন্ট হয়।

- তবে `n == 0` হলে রিকার্সন কাজ করে না, তাই আলাদা করে হ্যান্ডেল করা হয়েছে।

==========================
⏱ Time Complexity (TC): O(D)
    ➤ D = সংখ্যার digit সংখ্যা

💾 Space Complexity (SC): O(D)
    ➤ রিকার্সন কল stack-এর কারণে

==========================
*/
