#include <stdio.h>             // ইনপুট-আউটপুট ফাংশনের স্ট্যান্ডার্ড হেডার

int main()
{
    int n;                      // n → অ্যারেতে কতটি মান থাকবে
    scanf("%d", &n);            // ইউজার থেকে n ইনপুট

    int ar[n];                  // n সাইজের Variable Length Array (VLA) ডিক্লেয়ার

    // n-টি সংখ্যা ইনপুট নেওয়া হচ্ছে
    for (int i = 0; i < n; i++)
    {
        scanf("%d", &ar[i]);    // প্রতিটি মান ar[i]-তে রাখা
    }

    // ০-৫ পর্যন্ত গণনা রাখার কাউন্টারগুলো ইনিশিয়ালাইজ
    int cnt0 = 0, cnt1 = 0, cnt2 = 0, cnt3 = 0, cnt4 = 0, cnt5 = 0;

    // প্রতিটি উপাদান চেক করে সংশ্লিষ্ট কাউন্টার বাড়ানো
    for (int i = 0; i < n; i++)
    {
        if (ar[i] == 0)
        {
            cnt0++;
        }
        else if (ar[i] == 1)
        {
            cnt1++;
        }
        else if (ar[i] == 2)
        {
            cnt2++;
        }
        else if (ar[i] == 3)
        {
            cnt3++;
        }
        else if (ar[i] == 4)
        {
            cnt4++;
        }
        else if (ar[i] == 5)
        {
            cnt5++;
        }
    }

    // ফলাফল প্রিন্ট করা
    printf("%d -> %d\n", 0, cnt0);
    printf("%d -> %d\n", 1, cnt1);
    printf("%d -> %d\n", 2, cnt2);
    printf("%d -> %d\n", 3, cnt3);
    printf("%d -> %d\n", 4, cnt4);
    printf("%d -> %d\n", 5, cnt5);

    return 0;                   // প্রোগ্রাম সফলভাবে শেষ
}

/*
📥 Input:
10
1 2 0 2 0 3 1 2 5 0

📤 Output:
0 -> 3
1 -> 2
2 -> 3
3 -> 1
4 -> 0
5 -> 1

📝 Note:
এই প্রোগ্রামটি n-টি ইনপুট সংখ্যা থেকে কেবল ০-৫ মানগুলোর **ঘনত্ব (frequency)** গণনা করে দেখায়।  
যদি ৬ বা তার বেশি কোনো সংখ্যা ইনপুট দেওয়া হয়, সেটির জন্য আলাদা কাউন্টার নেই — তাই তা উপেক্ষা করা হয়। 😉
*/
