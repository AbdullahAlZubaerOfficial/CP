#include <stdio.h> // 📚 ইনপুট ও আউটপুট ফাংশনের জন্য হেডার ফাইল

int main() // 🏁 মেইন ফাংশন — প্রোগ্রামের শুরু
{
    int n;                  // 🔢 n: ইনপুট অ্যারেটির সাইজ
    scanf("%d", &n);        // 🧮 ইউজার থেকে n ইনপুট নেওয়া

    int ar[n];              // 🧺 n সাইজের অ্যারে ডিক্লেয়ার

    for (int i = 0; i < n; i++) // 🔁 অ্যারেতে nটি মান ইনপুট
    {
        scanf("%d", &ar[i]);    // ↪️ প্রতিটি মান ar[i]-তে রাখা
    }

    int idx;               // 🎯 idx: কোন ইনডেক্সের মান ডিলিট করতে হবে
    scanf("%d", &idx);     // ↪️ ইউজার ইনপুট নিচ্ছে কোন ইনডেক্স মুছবে

    // 🔄 ডিলিশনের জন্য ডান দিকের মানগুলো এক ধাপ বামে আনা হচ্ছে
    for (int i = idx; i < n - 1; i++)
    {
        ar[i] = ar[i + 1]; // ➡️ পরের মানটা বর্তমান ইনডেক্সে বসানো
    }

    n--; // 🧯 সাইজ ১ কমিয়ে দেওয়া হল কারণ একটা মান ডিলিট হয়েছে

    for (int i = 0; i < n; i++) // 🔁 রিমেইনিং মানগুলো প্রিন্ট
    {
        printf("%d ", ar[i]); // 📤 মানগুলো স্পেস দিয়ে প্রিন্ট
    }

    return 0; // ✅ প্রোগ্রাম সফলভাবে শেষ
}

/*
Input:
5
10 20 30 40 50
2

Output:
10 20 40 50
*/
