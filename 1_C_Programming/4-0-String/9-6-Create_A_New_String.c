#include <stdio.h>
#include <string.h>  // strlen() ফাংশনের জন্য হেডার ফাইল

int main()
{
    char s[1001], a[1001];  // দুইটা স্ট্রিং এর জন্য অ্যারে ডিক্লেয়ার

    gets(s);  // প্রথম স্ট্রিং ইনপুট নিচ্ছে (পুরো লাইন)
    gets(a);  // দ্বিতীয় স্ট্রিং ইনপুট নিচ্ছে (পুরো লাইন)

    int length1 = strlen(s); // প্রথম স্ট্রিং এর দৈর্ঘ্য বের করছে
    int length2 = strlen(a); // দ্বিতীয় স্ট্রিং এর দৈর্ঘ্য বের করছে

    printf("%d %d\n", length1, length2); // দুই স্ট্রিং এর দৈর্ঘ্য প্রিন্ট করছে
    printf("%s %s", s, a);  // দুই স্ট্রিং স্পেস দিয়ে একসাথে প্রিন্ট করছে

    return 0;  // প্রোগ্রাম সফলভাবে শেষ
}

/*
Input:
LEVEL
ONE

Output:
5 3
LEVEL ONE

📝 নোট:
- gets() ফাংশন সাধারণত ব্যবহারে সতর্ক থাকতে হয় কারণ এটি বাফার ওভারফ্লোর ঝুঁকি থাকে।
- ভবিষ্যতে fgets() ইউজ করা উচিত সেফটির জন্য।
- এখানে দুইটি ইনপুট পুরো লাইন হিসেবে নেয়া হয়েছে এবং দৈর্ঘ্য ও স্ট্রিং দুটোরই আউটপুট দেখানো হয়েছে।
*/
