#include <stdio.h> // ইনপুট-আউটপুট ফাংশনের জন্য হেডার ফাইল

int main() // মেইন ফাংশন শুরু
{
    int n; // সংখ্যা কতটি ডিজিট ইনপুট হবে, সেটা রাখার জন্য ভেরিয়েবল
    scanf("%d", &n); // ইউজার থেকে সংখ্যা নিচ্ছি — কতটি সংখ্যার ডিজিট ইনপুট হবে

    int sum = 0; // মোট যোগফল রাখার জন্য ভেরিয়েবল, শুরুতে ০

    char digit; // প্রতিটি সংখ্যা (digit) ইনপুট নেওয়ার জন্য ক্যারেক্টার টাইপ ভেরিয়েবল

    for (int i = 0; i < n; i++) // n বার লুপ চালাচ্ছি, কারণ n সংখ্যক ডিজিট ইনপুট হবে
    {
        scanf(" %c", &digit); 
        // প্রতিবার একটা ক্যারেক্টার ইনপুট নিচ্ছি
        // " " দিয়ে স্পেস দেওয়া হয়েছে যেন আগের newline বা whitespace স্কিপ করে

        sum = sum + digit - '0'; 
        // digit ক্যারেক্টার হওয়ায় তাকে সংখ্যায় রূপান্তর করছি (digit - '0')
        // যেমন: '5' - '0' = 5

        // তারপর সেই সংখ্যাটিকে যোগ করছি sum এ
    }

    printf("%d", sum); // সব digit যোগ করে যে ফলাফল, তা প্রিন্ট করছি

    return 0; // প্রোগ্রাম শেষ
}

/*
📥 Input:
5
12345

📤 Output:
15

📝 Short Note:
👉 এই প্রোগ্রামটি প্রথমে বলে দেয় কয়টা ডিজিট ইনপুট নেওয়া হবে (n)।  
তারপর `n` সংখ্যক digit (একসাথে লেখা যেমন `12345`) character আকারে ইনপুট নেয়।  
প্রতিটি digit কে ASCII থেকে integer এ রূপান্তর করে যোগফল বের করে।

📌 উদাহরণ:
'1' → 49 (ASCII), '0' → 48  
তাই '1' - '0' = 1  
'2' - '0' = 2  
...  
সবকিছু যোগ করলে হয় 1+2+3+4+5 = 15 ✅
*/

