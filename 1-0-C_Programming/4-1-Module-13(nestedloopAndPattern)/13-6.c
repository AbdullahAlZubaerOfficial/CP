#include <stdio.h>    // ইনপুট-আউটপুট ফাংশনগুলোর জন্য স্ট্যান্ডার্ড হেডার ফাইল

int main() { 
    int n;                      // কতটি সংখ্যা ইনপুট নিব, তা রাখবে
    scanf("%d", &n);            // n ইনপুট নিচ্ছি

    int ar[n];                  // n সাইজের একটি অ্যারে ডিক্লেয়ার করছি

    for (int i = 0; i < n; i++) {     // n বার লুপ চালাচ্ছি
        scanf("%d", &ar[i]);          // প্রতিবার একটা করে সংখ্যা ইনপুট নিচ্ছি
    }

    int x;                     // টার্গেট সমষ্টি (যে দুই সংখ্যার যোগফল চাই)
    scanf("%d", &x);           // x ইনপুট নিচ্ছি

    // দুইটি ভিন্ন ইনডেক্সের মান যোগ করে x হয় কিনা তা পরীক্ষা
    for (int i = 0; i < n; i++) {
        for (int j = i + 1; j < n; j++) {     // i এর পরের সব ইনডেক্স চেক
            if (ar[i] + ar[j] == x) {         // যদি যোগফল x হয়
                printf("yes\n");              // মিল পেলে "yes" প্রিন্ট
                return 0;                     // প্রোগ্রাম শেষ
            }
        }
    }

    printf("no\n");   // কোনো জোড়া মিলে না গেলে "no" প্রিন্ট
    return 0;         // প্রোগ্রাম সফলভাবে শেষ
}

/*
📥 Input:
5
1 4 2 3 5
6

📤 Output:
yes

📝 Short Note:
- এই প্রোগ্রামটি দেখে কোনো দুইটি ভিন্ন উপাদানের যোগফল নির্দিষ্ট মান (x) হয় কিনা।
- মিল পাওয়া মাত্রই "yes" প্রিন্ট করে প্রোগ্রাম শেষ হয়; না পেলে শেষে "no" প্রিন্ট হয়।
- সময় জটিলতা O(n²) কারণ নেস্টেড লুপে প্রতিটি জোড়া পরীক্ষা করা হয়।
*/
