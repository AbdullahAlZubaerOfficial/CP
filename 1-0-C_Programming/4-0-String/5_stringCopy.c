#include <stdio.h>          // ইনপুট-আউটপুট ফাংশনের জন্য হেডার ফাইল
#include <string.h>         // strlen() ফাংশন ব্যবহারের জন্য হেডার ফাইল

int main()
{
    char a[101], b[101];           // দুটি স্ট্রিং ইনপুট রাখার জন্য অ্যারে

    scanf("%s %s", a, b);          // দুটি স্ট্রিং ইনপুট নিচ্ছে (স্পেস ছাড়া)

    int lengthB = strlen(b);      // b স্ট্রিংয়ের দৈর্ঘ্য বের করছে

    for (int i = 0; i < lengthB; i++)   // b এর প্রতিটি ক্যারেক্টার কপি করছে
    {
        a[i] = b[i];                   // b[i] -> a[i] তে কপি করছে
    }

    printf("%s %s", a, b);       // দুইটি স্ট্রিং প্রিন্ট করছে

    return 0;                    // প্রোগ্রাম সফলভাবে শেষ
}

/*
Input:
abcd
jklm

Output:
jklm jklm

Note:
🔹 এখানে আমরা `b` স্ট্রিং-এর প্রতিটি ক্যারেক্টার `a`-এর মধ্যে কপি করছি  
🔹 কিন্তু `\0` (null character) কপি না করায় `a` স্ট্রিংটি পুরোপুরি null-terminated না 😬  
🔹 ফলে `a` এর শেষে আগের `abcd` এর ক্যারেক্টারও থেকে যেতে পারে — ভালোভাবে কাজ করতে চাইলে `a[lengthB] = '\0';` যোগ করা উচিত ✅  
🔹 এই কাজটা `strcpy(a, b);` দিয়েও করা যায় 💡  
*/
