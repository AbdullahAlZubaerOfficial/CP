#include <stdio.h>  // ইনপুট-আউটপুট ফাংশনের জন্য হেডার ফাইল

int main()
{
    char s[100001];                     // স্ট্রিং ইনপুট রাখার জন্য অ্যারে
    fgets(s, 100000, stdin);            // পুরো লাইন ইনপুট নিচ্ছে, including spaces

    for (int i = 0; s[i] != '\0'; i++)  // স্ট্রিংয়ের শেষ পর্যন্ত ঘুরবে
    {
        if (s[i] == '\\')               // যদি ব্যাকস্ল্যাশ পায়, তাহলে লুপ থেমে যাবে
        {
            break;
        }
        printf("%c", s[i]);             // ব্যাকস্ল্যাশ আসার আগ পর্যন্ত ক্যারেক্টার প্রিন্ট করবে
    }

    return 0;                           // প্রোগ্রাম সফলভাবে শেষ 😊
}

/*
Input:
This is a test\Ignore this part

Output:
This is a test

Note:
🔹 এই প্রোগ্রামটি একটি লাইন ইনপুট নেয় `fgets()` দিয়ে  
🔹 ইনপুটের মাঝে যেখানেই `\` দেখা যাবে, সেখান থেকে প্রিন্ট বন্ধ করে দেয়  
🔹 অর্থাৎ, ব্যাকস্ল্যাশ পর্যন্ত অংশটি শুধু প্রিন্ট হয় 📜  
🔹 যেমন: Input = "This is a test\\Ignore", Output = "This is a test" 😎
*/
