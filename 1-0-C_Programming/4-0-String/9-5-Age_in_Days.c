#include <stdio.h> // ইনপুট-আউটপুট ফাংশনের জন্য হেডার ফাইল

int main()
{
    int n;
    scanf("%d", &n); // ইউজার থেকে দিন সংখ্যা ইনপুট নিচ্ছে

    int years = n / 365;           // ১ বছরে ৩৬৫ দিন, তাই কত বছর হয় সেটা বের করছে
    int remaining_days = n % 365;  // পুরো বছর বাদ দিয়ে বাকি যতগুলো দিন থাকে

    int month = remaining_days / 30; // প্রতি মাসে ৩০ দিন ধরেই কত মাস হচ্ছে সেটা বের করছে
    int days = remaining_days % 30;  // মাস বাদ দিয়ে বাকি যেটুকু দিন থাকে

    printf("%d years\n", years);  // বছর প্রিন্ট করছে
    printf("%d months\n", month); // মাস প্রিন্ট করছে
    printf("%d days\n", days);    // দিন প্রিন্ট করছে

    return 0; // প্রোগ্রাম শেষ
}

/*

📥 Input:
400

📤 Output:
1 years
1 months
5 days

📝 সংক্ষিপ্ত ব্যাখ্যা:
- ৪০০ দিনকে আগে ৩৬৫ দিয়ে ভাগ করলে ১ বছর পাওয়া যায় ✅
- বাকি থাকে ৩৫ দিন (৪০০ % ৩৬৫ = ৩৫) ⏳
- এরপর ৩৫ দিনকে ৩০ দিয়ে ভাগ করলে ১ মাস পাওয়া যায় ✅
- বাকি থাকে ৫ দিন (৩৫ % ৩০ = ৫) ✅

📌 তাই final result:
👉 1 year
👉 1 month
👉 5 days

*/

