#include <stdio.h>      // ইনপুট-আউটপুটের জন্য হেডার ফাইল 😊
#include <string.h>     // strlen() ফাংশনের জন্য হেডার ফাইল

int main()
{
    char s[101];             // স্ট্রিং ইনপুট রাখার জন্য অ্যারে (সর্বোচ্চ ১০০ ক্যারেক্টার)

    scanf("%s", s);          // স্ট্রিং ইনপুট নিচ্ছে (স্পেস ছাড়া)

    int length = strlen(s);  // স্ট্রিংয়ের দৈর্ঘ্য বের করছে

    printf("%d", length);    // দৈর্ঘ্য প্রিন্ট করছে

    return 0;                // প্রোগ্রাম সফলভাবে শেষ 😊
}

/*
Input:
abdullah

Output:
8

Note:
🔹 এখানে আমরা `%s` ব্যবহার করেছি কারণ ইনপুটটা স্ট্রিং 💡  
🔹 `strlen()` ফাংশন দিয়ে স্ট্রিংয়ের মোট অক্ষরের সংখ্যা নির্ণয় করা হয়  
🔹 স্পেস থাকলে `scanf("%s", ...)` শুধু প্রথম অংশটাই নেয় — স্পেসসহ ইনপুটের জন্য `fgets()` ব্যবহার করতে হবে 😉
*/
