#include <stdio.h>      // ইনপুট-আউটপুট ফাংশনের জন্য হেডার ফাইল
#include <string.h>     // strlen ফাংশনের জন্য হেডার ফাইল

int main()
{
    char s[1000001];    // বড় সংখ্যা রাখার জন্য স্ট্রিং অ্যারে
    scanf("%s", s);     // স্ট্রিং হিসেবে সংখ্যা ইনপুট নিচ্ছে

    int length = strlen(s);  // স্ট্রিংয়ের দৈর্ঘ্য বের করছে

    int sum = 0;        // ডিজিটগুলোর যোগফল রাখার জন্য ভ্যারিয়েবল

    for (int i = 0; i < length; i++)
    {
        sum += s[i] - '0';  // প্রতিটি ক্যারেক্টারকে সংখ্যায় রূপান্তর করে যোগ করছে
        // '0' = ASCII 48, তাই s[i] - 48 দিলেও একই কাজ করে
    }

    printf("%d", sum);  // ডিজিটগুলোর যোগফল প্রিন্ট করছে

    return 0;           // প্রোগ্রাম সফলভাবে শেষ 😊
}

/*
Input:
359

Output:
17

Note:
🔹 এই প্রোগ্রামটি একটি সংখ্যাকে স্ট্রিং আকারে ইনপুট নেয়  
🔹 তারপর প্রতিটি ডিজিটকে সংখ্যায় কনভার্ট করে (s[i] - '0')  
🔹 সব ডিজিট যোগ করে একটি মোট যোগফল প্রিন্ট করে  
🔹 যেমন: 3 + 5 + 9 = 17 🎯
*/
