#include <stdio.h>

int main() { 
     
    int a[3][5];      // 3 rows and 5 columns এর 2D অ্যারে ডিক্লেয়ার

    a[1][2] = 10;    // ২য় রো (index 1) এবং ৩য় কলাম (index 2) এর ভ্যালু 10 সেট করলাম
    printf("%d", a[1][2]);  // সেট করা ভ্যালু প্রিন্ট করব: 10
   
    return 0;
}

/*

🔸 Input:
[কোনো ইনপুট নেই, প্রোগ্রামেই ভ্যালু সেট করা হয়েছে]

🔸 Output:
10

🔹 Short Note:
- `a` হল 2D অ্যারে যার সাইজ 3x5।
- `a[1][2]` দ্বারা আমরা ২য় রো এবং ৩য় কলামের উপাদান অ্যাক্সেস করছি।
- প্রোগ্রামে সরাসরি `a[1][2]` এর মান 10 সেট করে প্রিন্ট করেছি।

🔸 Time Complexity (TC):
O(1) - কারণ শুধু একটি এলিমেন্ট অ্যাক্সেস করা হয়েছে।

🔸 Space Complexity (SC):
O(1) - অ্যারে স্থির সাইজের এবং মেমোরি খরচ কম।

*/
