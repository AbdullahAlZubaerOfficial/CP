#include <stdio.h>
#include <string.h>

int main() { 
   
    int t;
    scanf("%d",&t); // টেস্ট কেস সংখ্যা ইনপুট নেওয়া হচ্ছে
    
    while (t--) // প্রতিটি টেস্ট কেসের জন্য লুপ
    {
        char s[100005]; // স্ট্রিং ইনপুট রাখার জন্য অ্যারে

        scanf("%s",s); // স্ট্রিং ইনপুট নেওয়া হচ্ছে

        int length = strlen(s); // স্ট্রিংয়ের দৈর্ঘ্য বের করা হচ্ছে

        int good = 0; // প্যাটার্ন পাওয়া গেছে কিনা, সেটি ট্র্যাক করার জন্য ফ্ল্যাগ

        // স্ট্রিংয়ে '010' বা '101' সাবস্ট্রিং আছে কিনা চেক করা হচ্ছে
        for(int i=0; i<length-2; i++){
            if((s[i]=='0' && s[i+1]=='1' && s[i+2]=='0') ||
               (s[i]=='1' && s[i+1]=='0' && s[i+2]=='1')){
                good = 1;
                break;
            }
        }

        // ফ্ল্যাগ অনুযায়ী আউটপুট
        if(good==1){
            printf("Good\n");
        }
        else{
            printf("Bad\n");
        }
    }

    return 0;
}


/*

🔸 Input:
3
0110
101
111000

🔸 Output:
Good
Good
Bad

🔹 Short Note:
প্রতিটি ইনপুট স্ট্রিং-এ '010' অথবা '101' সাবস্ট্রিং আছে কিনা তা খুঁজে বের করে।
যদি থাকে তবে "Good", না থাকলে "Bad" প্রিন্ট করে।

🔸 Time Complexity (TC):
O(n) প্রতি টেস্ট কেসের জন্য  
=> মোট TC: O(t * n), যেখানে t = টেস্ট কেস সংখ্যা, n = স্ট্রিং এর দৈর্ঘ্য

🔸 Space Complexity (SC):
O(1) (ইনপুট ছাড়া কোন অতিরিক্ত মেমোরি ব্যবহৃত হয়নি)

*/
