#include <stdio.h>   // ইনপুট-আউটপুট ফাংশনের জন্য হেডার ফাইল 📚

int main() { 
    long long n;                       // মোট কতটি সংখ্যা ইনপুট নেব, সেটি ধরে রাখে 🤔
    scanf("%lld", &n);                 // n এর মান ইউজার থেকে নেওয়া হচ্ছে ⌨️

    long long ar[n];                   // n সাইজের অ্যারে তৈরি করা হল 🗃️

    for (int i = 0; i < n; i++) {      // n বার লুপ চালিয়ে প্রতিটি সংখ্যা নেবে 🔄
        scanf("%lld", &ar[i]);         // প্রতিটি সংখ্যাকে অ্যারেতে রাখছে 📝
    }

    long long divided2 = 0, divided3 = 0; // ২-এ এবং ৩-এ নিঃশেষে বিভাজ্য সংখ্যার কাউন্টার ⏱️

    for (int i = 0; i < n; i++) {         // সব সংখ্যাই এখন পরীক্ষা করছি 🔍
        if (ar[i] % 2 == 0) {             // যদি ২-এ নিঃশেষে বিভাজ্য হয় ➗2
            divided2++;                   // divided2 কাউন্টার ১ বাড়ালাম ➕
        }
        else if (ar[i] % 3 == 0) {        // না হলে যদি ৩-এ নিঃশেষে বিভাজ্য হয় ➗3
            divided3++;                   // divided3 কাউন্টার ১ বাড়ালাম ➕
        }
        /* এই ব্লকটি কখনও চলবে না, কারণ ২-এ বিভাজ্য সংখ্যা 
           আগেই প্রথম if-এ ধরা পড়ে যায় 😅                        */
        else if (ar[i] % 2 == 0 && ar[i] % 3 == 0) {
            printf("2");                  // (অপ্রয়োজনীয়) আউটপুট ⚠️
        }
    }

    printf("%lld %lld", divided2, divided3); // দুইটি কাউন্টার এক সাথে প্রিন্ট 🖨️

    return 0;                            // প্রোগ্রাম সফলভাবে শেষ ✅
}

/*
Input:
6
2 3 6 9 12 7

Output:
3 2

Note: প্রোগ্রামটি প্রথমে n সংখ্যক পূর্ণসংখ্যা ইনপুট নিয়ে গণনা করে—
      🔸 কতগুলো সংখ্যা ২-এ নিঃশেষে বিভাজ্য,
      🔸 (আর ২-এ নয় এমন) কতগুলো সংখ্যা ৩-এ নিঃশেষে বিভাজ্য।
      শেষে দুইটি কাউন্টার স্পেস-সেপারেটেড আউটপুট দেয়। 🚀
*/
