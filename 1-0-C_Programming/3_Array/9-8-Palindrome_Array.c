#include <stdio.h>        // স্ট্যান্ডার্ড ইন-আউটপুট হেডার ফাইল 📚

int main()
{
    long long n;                       // n = মোট উপাদান সংখ্যা 🧮
    scanf("%lld", &n);                 // n ইনপুট নিচ্ছি ⌨️

    long long ar[n];                   // n সাইজের অ্যারে ডিক্লেয়ার 📦
    for (int i = 0; i < n; i++)        // ০ → n-১ পর্যন্ত লুপ চালিয়ে
    {
        scanf("%lld", &ar[i]);         // প্রতিটি মান ইনপুট নিচ্ছি 📝
    }

    /* 🔄 প্যালিনড্রোম চেক:
       সামনের দিকের ar[i] আর পিছনের ar[n-1-i] তুলনা করি */
    for (int i = 0; i < n / 2; i++)    // অর্ধেক পর্যন্ত যাচাইই যথেষ্ট 🚶‍♂️
    {
        if (ar[i] != ar[n - 1 - i])    // জুটি না মিললে ❌
        {
            printf("NO\n");            // প্যালিনড্রোম নয় 🙅‍♀️
            return 0;                  // এখানেই প্রোগ্রাম শেষ 🛑
        }
    }

    printf("YES\n");                   // সব জুটি মিলে গেলে প্যালিনড্রোম ✅
    return 0;                          // সফল সমাপ্তি 🏁
}

/*
Input:
5
1 2 3 2 1

Output:
YES

🔎 Short Note:
• এই প্রোগ্রামটি প্রথম ও শেষ, দ্বিতীয় ও শেষ-দ্বিতীয় … এভাবে
  দু-পাশ থেকে তুলনা করে অ্যারে-টি প্যালিনড্রোম কি না যাচাই করে।  
• কোনো জোড়া অমিল পেলেই সঙ্গে সঙ্গে “NO” ছাপায়; যদি সব জোড়া মিলে যায়,
  “YES” দেয়। সময় জটিলতা O(n) ⏱️
*/
