#include <stdio.h>              // 📚 স্ট্যান্ডার্ড ইনপুট-আউটপুট হেডার

int main()
{
    int n;                      // ✏️ কতটি সংখ্যা ইনপুট নেওয়া হবে
    scanf("%d", &n);            // 👉 ইউজারের কাছ থেকে n ইনপুট

    int ar[n];                  // 📦 Variable Length Array (VLA) n সাইজের

    // ➡️ n-টি ইনপুট সংখ্যা অ্যারেতে রাখছি
    for (int i = 0; i < n; i++)
    {
        scanf("%d", &ar[i]);    // 🔢 প্রত্যেকটি মান ar[i] তে সেভ
    }

    int fre[6] = {0};           // 🗄️ 0-৫ এর ফ্রিকোয়েন্সি রাখার জন্য ৬ সাইজের অ্যারে 0 দিয়ে ইনিশিয়ালাইজ

    // 🔄 প্রতিটি ইনপুট নম্বর ঘুরে ফ্রিকোয়েন্সি বাড়াচ্ছি
    for (int i = 0; i < n; i++)
    {
        if      (ar[i] == 0) fre[0]++;  // 0 পাওয়া গেলে fre[0] বাড়াও
        else if (ar[i] == 1) fre[1]++;  // 1 হলে fre[1]++
        else if (ar[i] == 2) fre[2]++;  // 2 হলে fre[2]++
        else if (ar[i] == 3) fre[3]++;  // 3 হলে fre[3]++
        else if (ar[i] == 4) fre[4]++;  // 4 হলে fre[4]++
        else if (ar[i] == 5) fre[5]++;  // 5 হলে fre[5]++
    }

    // 📤 ফলাফল প্রিন্ট
    printf("%d -> %d\n", 0, fre[0]);
    printf("%d -> %d\n", 1, fre[1]);
    printf("%d -> %d\n", 2, fre[2]);
    printf("%d -> %d\n", 3, fre[3]);
    printf("%d -> %d\n", 4, fre[4]);
    printf("%d -> %d\n", 5, fre[5]);

    return 0;                   // 🏁 প্রোগ্রাম সফলভাবে শেষ
}

/*
📥 Input:
10
1 2 0 2 0 3 1 2 5 0

📤 Output:
0 -> 3
1 -> 2
2 -> 3
3 -> 1
4 -> 0
5 -> 1

📝 Note: এই প্রোগ্রামটি ০-৫ পর্যন্ত সংখ্যার ঘনত্ব (frequency) গণনা করে।  
৬ কিংবা তার বেশি কোনো সংখ্যা দিলে সেটা ইগনোর হয়ে যাবে — চাইলে fre অ্যারের সাইজ বাড়িয়ে সাবধানে হ্যান্ডেল করতে পারো! 😉🤖
*/
