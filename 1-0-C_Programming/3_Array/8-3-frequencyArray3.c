#include <stdio.h>

int main()
{
    int n;
    scanf("%d", &n); // ইউজার থেকে n ইনপুট নিচ্ছি

    int ar[n];       // n সাইজের অ্যারে নিচ্ছি

    // n সংখ্যার ইনপুট নিচ্ছি
    for (int i = 0; i < n; i++)
    {
        scanf("%d", &ar[i]);
    }

    int fre[6] = {0}; // ০-৫ পর্যন্ত ফ্রিকোয়েন্সি গোনার জন্য অ্যারে

    // প্রত্যেকটি সংখ্যার ফ্রিকোয়েন্সি বাড়ানো
    for (int i = 0; i < n; i++)
    {
        fre[ar[i]]++; // সরাসরি ইনডেক্স ধরে ফ্রিকোয়েন্সি ইনক্রিমেন্ট ✅
    }

    // আউটপুট প্রিন্ট করা
    for (int i = 0; i < 6; i++)
    {
        printf("%d -> %d\n", i, fre[i]);
    }

    return 0;
}

/*
📥 Input:
10
1 2 0 2 0 3 1 2 5 0

📤 Output:
0 -> 3
1 -> 2
2 -> 3
3 -> 1
4 -> 0
5 -> 1

✅ এই কোডের ভালো দিক:
- `fre[ar[i]]++` দিয়ে সরাসরি মানের উপর ভিত্তি করে কাউন্ট করা হইছে
- কম কোডে বেশি কার্যকর 💪

⚠️ ছোট্ট সাবধানতা:
তুমি যদি ৫ এর বেশি মান ইনপুট দাও, তাহলে `fre[ar[i]]` ইনডেক্স অ্যারেতে নাই — তাই `Segmentation Fault` দিতে পারে।  
👉 তাই ইউজার ইনপুট ০-৫ এর ভিতরে আছে কিনা চেক করলে ভালো হয় 😊

*/
