#include <stdio.h> // Standard input-output লাইব্রেরি include করা হয়েছে

int main()
{
  int n; // ইউজার কতটি সংখ্যা দিবে তা রাখার জন্য ভেরিয়েবল
  scanf("%d", &n); // ইউজার থেকে সংখ্যা ইনপুট নেওয়া

  int ar[n]; // n সাইজের একটি অ্যারে ডিক্লেয়ার করা হয়েছে

  // অ্যারেতে ইউজারের ইনপুট নেওয়ার জন্য লুপ
  for (int i = 0; i < n; i++)
  {
    scanf("%d", &ar[i]); // প্রতিটি ইনপুট ar[i] তে রাখা হচ্ছে
  }

  int sum = 0; // sum ভেরিয়েবল ইনিশিয়ালাইজ করা হয়েছে 0 দিয়ে

  // অ্যারেতে থাকা সব সংখ্যার যোগফল বের করার জন্য লুপ
  for (int i = 0; i < n; i++)
  {
    sum = sum + ar[i]; // প্রতিটি উপাদান sum এ যোগ করা হচ্ছে
  }

  printf("%d ", sum); // সর্বমোট যোগফল প্রিন্ট করা হচ্ছে

  return 0; // প্রোগ্রাম সফলভাবে শেষ
}

/*
📥 Input:
5
1 2 3 4 5

📤 Output:
15

📝 Note:
এই প্রোগ্রামটি ইউজার থেকে প্রথমে একটি সংখ্যা n নেয়, যেটি অ্যারেতে কতটি উপাদান থাকবে সেটা নির্ধারণ করে।
তারপর n সংখ্যক ইনপুট নিয়ে, সব উপাদান যোগ করে মোট যোগফল প্রিন্ট করে।

👉 উদাহরণ হিসেবে,
1 + 2 + 3 + 4 + 5 = 15 ✅

🔢 Useful concept: Variable Length Array (VLA)
*/
