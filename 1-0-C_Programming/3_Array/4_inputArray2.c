#include <stdio.h> // Standard input-output library

int main()
{
    int a[5]; // ৫টি integer ধারণ করার জন্য একটি array ডিক্লেয়ার করা হয়েছে

    // ইউজার থেকে ৫টি সংখ্যা ইনপুট নেওয়ার জন্য লুপ চালানো হয়েছে
    for (int i = 0; i <= 4; i++)
    {
        scanf("%d", &a[i]); // প্রতিবার a[i] তে একটি সংখ্যা নেওয়া হচ্ছে
    }

    // array এর সবগুলো উপাদান প্রিন্ট করার জন্য আরেকটি লুপ
    for (int i = 0; i <= 4; i++)
    {
        printf("%d ", a[i]); // প্রতিবার a[i] প্রিন্ট হচ্ছে
    }

    return 0; // প্রোগ্রাম সফলভাবে শেষ
}


/*
📥 Input:
10 20 30 40 50

📤 Output:
10 20 30 40 50

📝 Note:
এই প্রোগ্রামটি ৫টি সংখ্যা ইনপুট নিয়ে সেগুলো আবার একসাথে প্রিন্ট করে দেয়।

🔁 এখানে দুটি loop ব্যবহার করা হয়েছে:
- প্রথমটি ইনপুট নেওয়ার জন্য
- দ্বিতীয়টি আউটপুট দেওয়ার জন্য

*/