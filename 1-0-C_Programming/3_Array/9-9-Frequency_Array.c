#include "stdio.h"                // স্ট্যান্ডার্ড ইনপুট-আউটপুট লাইব্রেরি 📚

int main()
{
    int a, b;                     // a = ইনপুট সংখ্যাগুলোর সংখ্যা, b = সর্বোচ্চ সংখ্যাটি 📊
    scanf("%d %d", &a, &b);       // ইউজার থেকে a ও b ইনপুট নিচ্ছি ⌨️

    int ar[a];                   // a সাইজের একটি অ্যারে তৈরি করলাম 📦

    for (int i = 0; i < a; i++)  // সবগুলো ইনপুট অ্যারেতে রাখছি 🔄
    {
        scanf("%d", &ar[i]);     // প্রতিটি ইনপুট ar[] তে রাখছি 📝
    }

    int freq[b + 1];             // ফ্রিকোয়েন্সি অ্যারে, যাতে ইনডেক্স ১ → b পর্যন্ত চলতে পারে 🧮

    for (int i = 1; i <= b; i++) // freq[] এর সব ঘর ০ দিয়ে ইনিশিয়ালাইজ করছি 🔁
    {
        freq[i] = 0;
    }

    for (int i = 0; i < a; i++)  // মূল ar[] থেকে মান পড়ে freq[] তে কাউন্ট বাড়াচ্ছি 📊
    {
        freq[ar[i]]++;           // যে সংখ্যাটি পাওয়া যাচ্ছে, তার কাউন্ট বাড়াও ➕
    }

    for (int i = 1; i <= b; i++) // ১ থেকে b পর্যন্ত প্রতিটি সংখ্যার ফ্রিকোয়েন্সি প্রিন্ট করছি 📤
    {
        printf("%d\n", freq[i]); // ফলাফল দেখাচ্ছে ➡️
    }

    return 0;                    // প্রোগ্রাম সফলভাবে শেষ ✅
}

/*

Input:
7 5
1 2 1 3 2 1 5

Output:
3
2
1
0
1

Short Note:
এই প্রোগ্রামটি ১ থেকে b পর্যন্ত প্রতিটি সংখ্যার কতবার এসেছে তা গণনা করে দেখায়।

freq[] অ্যারে ব্যবহার করে ইনপুট সংখ্যা গুলোর ঘনত্ব/ফ্রিকোয়েন্সি হিসাব করা হয়।

টাইম কমপ্লেক্সিটি: O(a + b)

কারণ প্রথমে a বার ইনপুট, তারপর b বার আউটপুট — মোট O(n) টাইমে কাজ করে ⏱️

এই ধরণের গাণিতিক বিশ্লেষণকে ফ্রিকোয়েন্সি কাউন্টিং বলা হয় 🔢


*/
