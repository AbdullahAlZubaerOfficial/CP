#include <stdio.h> // 🌟 Input-output function-এর হেডার

int main() { 
    int n;
    scanf("%d", &n); // 📥 অ্যারের সাইজ ইনপুট

    int ar[n];       // 🧮 সাইজ অনুযায়ী অ্যারে ডিক্লেয়ার

    // 🔁 অ্যারে ইনপুট নেয়া
    for(int i = 0; i < n; i++){
        scanf("%d", &ar[i]);
    }

    int left = 0, right = n - 1; // 📍 দুই দিক থেকে সূচক সেট

    // 🔁 দুই দিক থেকে এক এক করে প্রিন্ট করা
    while(left <= right){
        printf("%d ", ar[left++]); // ⬅️ বাম দিকের সংখ্যা প্রিন্ট ও ইনক্রিমেন্ট

        if(left <= right){ // ✅ এখনো মাঝখানে আসেনি
            printf("%d ", ar[right--]); // ➡️ ডান দিকের সংখ্যা প্রিন্ট ও ডিক্রিমেন্ট
        }
    }

    return 0; // ✅ প্রোগ্রাম শেষ
}


/*
==========================
📥 Input:
6
1 2 3 4 5 6

📤 Output:
1 6 2 5 3 4

📥 Input:
5
10 20 30 40 50

📤 Output:
10 50 20 40 30

==========================
🧠 Short Note:
- অ্যারেটাকে দুই দিক থেকে এক এক করে প্রিন্ট করা হয়েছে:
  ➤ প্রথমে বাম দিক থেকে একটা
  ➤ তারপর ডান দিক থেকে একটা
  ➤ এভাবে যতক্ষণ না পর্যন্ত `left > right` হয়ে যায়
  
- ফলে আউটপুট হয় zig-zag বা ভিতর দিকে সোজা আসা স্টাইলে।

==========================
⏱ Time Complexity (TC): O(n)
    ➤ প্রতিটি উপাদান একবার প্রিন্ট হয়

💾 Space Complexity (SC): O(1)
    ➤ অতিরিক্ত কোনো মেমোরি ব্যবহার হয়নি

==========================
*/
