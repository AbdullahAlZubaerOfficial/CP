#include <stdio.h>  // ইনপুট-আউটপুট ব্যবহারের জন্য হেডার ফাইল

int main() { 
   int n, x, y;                  // n = কয়টা কেস, x ও y = প্রতি কেসের দুইটি সংখ্যা
   scanf("%d", &n);              // ইউজার থেকে কয়টা ইনপুট নিবে তা ইনপুট নিচ্ছে

   for(int i = 0; i < n; i++){   // n বার লুপ চলবে, প্রতি কেসের জন্য
      scanf("%d %d", &x, &y);    // দুইটি সংখ্যা ইনপুট নিচ্ছে x ও y

      int sum = 0;               // odd সংখ্যাগুলোর যোগফল রাখার জন্য ভেরিয়েবল

      // যদি x বড় হয় y এর থেকে, তাহলে তাদের অদল-বদল করে নিচ্ছে যেন x ছোট থাকে সবসময়
      if(x > y){
         int temp = x;
         x = y;
         y = temp;
      }

      // x আর y এর মাঝের (exclusive) সব সংখ্যার মধ্যে Odd গুলো যোগ করা হচ্ছে
      for(int j = x + 1; j < y; j++){
         if(j % 2 != 0){         // যদি সংখ্যা Odd হয়
            sum = sum + j;      // যোগ করা হচ্ছে
         }
      }

      printf("%d\n", sum);      // যোগফল প্রিন্ট করা হচ্ছে
   }

   return 0;  // প্রোগ্রাম সফলভাবে শেষ
}


/*
📥 Input:
3
6 10
1 5
3 3

📤 Output:
7
5
0

📝 Note:
এই প্রোগ্রামটি দুটি সংখ্যার মাঝখানের (exclusive) সকল Odd সংখ্যা বের করে তাদের যোগফল দেখায়।
যেমন, 6 থেকে 10 এর মাঝখানে Odd সংখ্যা হলো 7 ও 9 → যোগফল 16।

👉 যদি দুটি সংখ্যা সমান হয়, তাহলে কোন সংখ্যা মাঝখানে থাকবে না → যোগফল 0।
*/
