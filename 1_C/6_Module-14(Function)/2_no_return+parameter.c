#include <stdio.h>

// দুটি সংখ্যা প্যারামিটার হিসেবে নিয়ে তাদের যোগফল প্রিন্ট করে
void sum(int a, int b) {
    int ans = a + b;        // যোগফল হিসাব
    printf("%d", ans);      // ফলাফল প্রিন্ট
}

int main() { 
    int a, b;               // ইনপুট নেওয়ার জন্য দুইটি ভ্যারিয়েবল
    scanf("%d %d", &a, &b); // ইউজার থেকে ইনপুট নিচ্ছে
    sum(a, b);              // sum ফাংশন কল করে যোগফল প্রিন্ট করছে
   
    return 0;
}

/*
🔹 Input:
10 20

🔹 Output:
30

🔹 Short Note:
এই প্রোগ্রামটি ইউজারের কাছ থেকে দুটি সংখ্যা ইনপুট নিয়ে `sum()` ফাংশনে পাঠায় এবং ফাংশনটি তাদের যোগফল প্রিন্ট করে।

🔹 Time Complexity (TC): O(1)  
👉 কারণ, সব অপারেশন ধ্রুবক সময়ে হয়।

🔹 Space Complexity (SC): O(1)  
👉 কারণ, অতিরিক্ত জায়গা ব্যবহৃত হয়নি।
*/
