#include <stdio.h>                 // 📚 স্ট্যান্ডার্ড ইন-আউটপুট হেডার ফাইল

int main()                         // 🏁 মেইন ফাংশন — প্রোগ্রাম শুরু
{
   int n;                          // 🔢 n: কতটি সংখ্যা ইনপুট নেবো
   scanf("%d", &n);                // ➡️ n-এর মান ইনপুট

   int ar[n];                      // 📦 n সাইজের একটি Variable Length Array (VLA)

   for (int i = 0; i < n; i++)     // 🔁 0 … n-1 পর্যন্ত লুপ—অ্যারেতে মান নেওয়া
   {
      scanf("%d", &ar[i]);         //     ↳ প্রতিবার ar[i] তে একটি সংখ্যা সেভ
   }

   /* 🔄 অ্যারের মান উল্টানো (swap করে) */
   for (int i = 0, j = n - 1; i < j; i++, j--) // দুই দিক থেকে এগোই, যতক্ষণ i < j
   {
      int temp = ar[i];            //     ↳ temp-এ বাম দিকের মান ধরে রাখি
      ar[i] = ar[j];               //     ↳ ডান দিকের মানকে বামে আনি
      ar[j] = temp;                //     ↳ temp-এর মান ডানে বসাই
   }

   /*
   // উপরে for-লুপের বিকল্প হিসেবে while-লুপও ব্যবহার করা যায়:
   int i = 0;
   int j = n - 1;
   while (i < j)
   {
      int temp = ar[i];
      ar[i] = ar[j];
      ar[j] = temp;
      i++;      // বামে সূচক এক ধাপ এগোয়
      j--;      // ডানে সূচক এক ধাপ পিছোয়
   }
   */

   for (int i = 0; i < n; i++)     // 🔁 আবার 0 … n-1 — এবার প্রিন্ট
   {
      printf("%d ", ar[i]);        //     ↳ রিভার্স হওয়া মানগুলো প্রিন্ট
   }

   return 0;                       // ✅ প্রোগ্রাম সফলভাবে শেষ
}

/*
Input:
5
10 20 30 40 50

Output:
50 40 30 20 10
*/
