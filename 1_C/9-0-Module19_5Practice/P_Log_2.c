#include <stdio.h>  // 📘 স্ট্যান্ডার্ড ইনপুট-আউটপুট লাইব্রেরি

int main() {
    long long n;
    scanf("%lld", &n);  // 📥 ইউজার থেকে একটি বড় সংখ্যা ইনপুট নিচ্ছে

    int ans = 0;  // 🧮 ভাগ করার সংখ্যা গণনার জন্য কাউন্টার

    while (n > 1) {  // 🔁 যতক্ষণ পর্যন্ত n > 1, ততক্ষণ চলবে
        n /= 2;      // ➗ n কে 2 দিয়ে ভাগ করে
        ans++;       // ✅ প্রতিবার ভাগ করার পর কাউন্টার বাড়ায়
    }

    printf("%d\n", ans);  // 📤 যতবার ভাগ হয়েছে তা প্রিন্ট করে
    return 0;
}

/* =====================================================
📥 Input:
16

📤 Output:
4

🧠 Short Note:
এই প্রোগ্রামটি কোনো সংখ্যাকে 2 দিয়ে যতবার ভাগ করলে 1-এ পৌঁছায়, 
তা গণনা করে। এটি আসলে log₂(n) এর সমান মান প্রদান করে।

উদাহরণ:
n = 16 → 16→8→4→2→1 (4 বার ভাগ করতে হয়েছে)

⏱ Time Complexity (TC): O(log n)
   ▸ কারণ n প্রতি ধাপে অর্ধেক হয়ে যায়।

💾 Space Complexity (SC): O(1)
   ▸ কোনো অতিরিক্ত মেমোরি ব্যবহার হয়নি।

📌 Tip:
এটি binary search বা log base 2 সংক্রান্ত অনেক সমস্যার ভিত্তি হিসেবে কাজ করে।
===================================================== */
