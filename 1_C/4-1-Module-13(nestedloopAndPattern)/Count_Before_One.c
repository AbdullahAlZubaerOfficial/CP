#include <stdio.h>

// ফাংশন: 1 আসার আগে কয়টা সংখ্যা আছে, তা গণনা করে
int count_before_one(int ar[], int n){
    int count = 0;

    for(int i = 0; i < n; i++){
        if(ar[i] == 1){
            break; // 1 পেলে লুপ থেমে যাবে
        }
        count++; // 1 না হলে কাউন্ট বাড়বে
    }

    return count; // মোট কয়টা সংখ্যা 1 এর আগে আছে, তা রিটার্ন করে
}

int main() { 
    int n;
    scanf("%d", &n); // ইউজার থেকে অ্যারের সাইজ ইনপুট নেয়া হলো

    int ar[n]; // অ্যারে ডিক্লেয়ার

    for(int i = 0; i < n; i++){
        scanf("%d", &ar[i]); // অ্যারের উপাদান ইনপুট নিচ্ছে
    }

    int result = count_before_one(ar, n); // ফাংশন কল করে রেজাল্ট নেয়া

    printf("%d", result); // রেজাল্ট প্রিন্ট করা হলো
    return 0;
}

/*
📥 Input:
6
5 8 3 7 1 9

📤 Output:
4

📝 Short Note:
এই প্রোগ্রামটি একটি অ্যারে ইনপুট নেয় এবং অ্যারেতে প্রথমবার 1 আসার আগ পর্যন্ত কয়টা উপাদান আছে, তা গণনা করে প্রিন্ট করে। 
যেমন উপরের ইনপুটে `1` এর আগে আছে → 5, 8, 3, 7 = মোট 4 টি সংখ্যা। তাই আউটপুট 4।

⚠️ যদি অ্যারেতে কোথাও 1 না থাকে, তাহলে পুরো অ্যারের সব উপাদানকেই কাউন্ট করবে।
টাইম কমপ্লেক্সিটি: O(n)
*/
