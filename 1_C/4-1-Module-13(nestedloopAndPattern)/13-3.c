#include <stdio.h>   // ইনপুট-আউটপুট ফাংশনের জন্য হেডার ফাইল 😊

int main() { 
    int n;                          // পিরামিডের উচ্চতা (লাইন সংখ্যা) সংরক্ষণ করবে
    scanf("%d", &n);                // ইউজার থেকে n ইনপুট নিচ্ছি

    int space = n - 1;              // প্রথম লাইনে বামের দিকে (n-1) টা স্পেস লাগবে
    int star  = 1;                  // প্রথম লাইনে ১টা *

    for (int i = 1; i <= n; i++) {  // মোট n লাইন প্রিন্ট করতে লুপ
        /* ---------- স্পেস প্রিন্ট ---------- */
        for (int j = 1; j <= space; j++) {
            printf(" ");            // বাম দিকে প্রয়োজনীয় ফাঁকা জায়গা
        }
        /* ---------- তারকা প্রিন্ট ---------- */
        for (int j = 1; j <= star;  j++) {
            printf("*");            // ওই লাইনের প্রয়োজনীয় সংখ্যা অনুযায়ী *
        }
        printf("\n");               // এক লাইন শেষ, নিউলাইনে যাচ্ছি

        space--;                    // পরের লাইনের স্পেস ১টি কমবে
        star += 2;                  // পরের লাইনের জন্য তারকা ২টি বাড়বে
    }

    return 0; // প্রোগ্রাম সফলভাবে শেষ ✅
}

/*
📥 Input:
5

📤 Output:
    *
   ***
  *****
 *******
*********

📝 Short Note:
এই প্রোগ্রামটি n লাইনের একটি কেন্দ্র-সমমিত (centered) পিরামিড বা ত্রিভুজ ⭐ আঁকে।
- `space` ভ্যারিয়েবল ঠিক করে কতগুলো ফাঁকা জায়গা দিতে হবে যাতে পিরামিড মাঝখানে থাকে।
- `star` ভ্যারিয়েবল প্রতিটি লাইনে কতগুলো * প্রিন্ট হবে তা নিয়ন্ত্রণ করে (১, ৩, ৫, ...).
- প্রতি লাইনের শেষে `space--` ও `star += 2` করে পরবর্তী লাইনের জন্য মান আপডেট করা হয়।

এভাবে তুমি যেকোনো উচ্চতার সুন্দর পিরামিড প্রিন্ট করতে পারো! 🎉
*/
