#include <stdio.h> // ইনপুট-আউটপুটের জন্য stdio.h হেডার ফাইল ইনক্লুড করছি
#include <string.h> // strlen() ফাংশন ব্যবহারের জন্য string.h ইনক্লুড করছি

int main() // মেইন ফাংশন শুরু
{
    char s[1001]; // স্ট্রিং ইনপুট রাখার জন্য একটি ক্যারেক্টার অ্যারে ডিক্লেয়ার করলাম
    scanf("%s", s); // ইউজার থেকে স্ট্রিং ইনপুট নিচ্ছি

    int len = strlen(s); // স্ট্রিংয়ের দৈর্ঘ্য বের করছি

    // এখন প্রথম অক্ষর থেকে শুরু করে মাঝামাঝি পর্যন্ত চেক করব
    for (int i = 0; i < len / 2; i++) // শুধু অর্ধেক পর্যন্ত চেক করলেই হবে
    {
        if (s[i] != s[len - 1 - i]) // যদি বিপরীত দিকের অক্ষর একই না হয়
        {
            printf("NO\n"); // তাহলে এটা palindrome না, তাই NO প্রিন্ট
            return 0; // প্রোগ্রাম এখানেই শেষ করে দিচ্ছি
        }
    }

    printf("YES\n"); // যদি সব অক্ষর মিলে যায় তাহলে এটা palindrome, তাই YES প্রিন্ট
    return 0; // প্রোগ্রাম শেষ
}

/*
📥 Input 1:
abba

📤 Output 1:
YES

📥 Input 2:
icpcassiut

📤 Output 2:
NO

📝 Short Note:
Palindrome মানে হলো এমন একটা শব্দ বা স্ট্রিং যেটা উল্টে পড়লেও একই রকম শোনায়। যেমন:
- abba ➡️ উল্টো করলেও abba
- madam ➡️ উল্টো করলেও madam
এমন শব্দগুলো palindrome ✅

এই প্রোগ্রামে আমরা চেক করছি, স্ট্রিংয়ের প্রথম অক্ষর আর শেষ অক্ষর সমান কি না, তারপর দ্বিতীয় আর শেষ থেকে দ্বিতীয়... এভাবে সব অক্ষর মিলিয়ে দেখছি।
যদি সব মিলে, তাহলে সেটা palindrome — তাই YES প্রিন্ট হয়।
যদি একটাও না মিলে, তাহলে সেটা palindrome না — তাই NO প্রিন্ট হয়।
*/
