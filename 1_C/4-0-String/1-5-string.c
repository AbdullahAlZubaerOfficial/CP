#include <stdio.h>      // ইনপুট-আউটপুট ফাংশনের জন্য হেডার ফাইল

int main()
{
    char a[4];                          // ৪টি ক্যারেক্টার রাখার জন্য অ্যারে

    for (int i = 0; i < 4; i++)         // ৪ বার লুপ চালিয়ে ক্যারেক্টার ইনপুট নিচ্ছে
    {
        scanf("%c", &a[i]);             // প্রতিবার একটি করে ক্যারেক্টার ইনপুট নিচ্ছে
    }

    for (int i = 0; i < 4; i++)         // আবার ৪ বার লুপ চালিয়ে ইনপুটকৃত ক্যারেক্টার প্রিন্ট করছে
    {
        printf("%c ", a[i]);            // প্রতিটি ক্যারেক্টার প্রিন্ট করছে স্পেসসহ
    }

    return 0;                           // প্রোগ্রাম সফলভাবে শেষ
}

/*
Input:
zubaer

Output:
z u b a 

Note:
এই প্রোগ্রামটি ইউজার থেকে ৪টি ক্যারেক্টার ইনপুট নেয় এবং সেগুলো স্পেস দিয়ে প্রিন্ট করে।  
⚠️ কিন্তু যদি ইনপুটে স্পেস, নিউলাইন বা অতিরিক্ত ক্যারেক্টার থাকে, সেগুলোও ইনপুট হিসেবে গণ্য হবে।  
📌 যেমন: "zubaer" দিলে প্রথম ৪টি ক্যারেক্টার 'z', 'u', 'b', 'a' নিবে — বাকি 'e', 'r' নেয় না।
*/
