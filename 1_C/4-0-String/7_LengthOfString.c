#include <stdio.h>  // ইনপুট-আউটপুট ফাংশনের জন্য হেডার ফাইল

int main()
{
    char s[101];           // স্ট্রিং ইনপুট রাখার জন্য অ্যারে

    scanf("%s", s);        // স্ট্রিং ইনপুট নিচ্ছে (স্পেস ছাড়া)

    int count = 0;         // স্ট্রিংয়ের দৈর্ঘ্য গণনার জন্য কাউন্টার

    for (int i = 0; s[i] != '\0'; i++)  // যতক্ষণ না null character আসে
    {
        count++;           // প্রতিটি ক্যারেক্টারের জন্য কাউন্ট বাড়ায়
    }

    printf("%d", count);   // স্ট্রিংয়ের দৈর্ঘ্য প্রিন্ট করে

    return 0;              // প্রোগ্রাম সফলভাবে শেষ 😊
}

/*
Input:
Abdullah

Output:
8

Note:
🔹 এখানে আমরা `strlen()` ফাংশন ব্যবহার না করে নিজে হাতে স্ট্রিংয়ের দৈর্ঘ্য বের করেছি  
🔹 প্রতিটি ক্যারেক্টার দেখে কাউন্ট বাড়িয়েছি যতক্ষণ না `'\0'` (null character) পাই  
🔹 এটি একটি ভালো প্র্যাকটিস যাতে বোঝা যায় কিভাবে স্ট্রিং কাজ করে C-তে 💡
*/
