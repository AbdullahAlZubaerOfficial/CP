#include <stdio.h>      // ইনপুট-আউটপুট ফাংশনের জন্য হেডার ফাইল
#include <string.h>     // স্ট্রিং ফাংশনের জন্য হেডার ফাইল

int main()
{
    char s[50];                      // একটি স্ট্রিং ইনপুট রাখার জন্য অ্যারে (সর্বোচ্চ ৫০ অক্ষর)

    // gets(s);                      // ⚠️ পুরনো ফাংশন, নিরাপদ নয়
    fgets(s, 50, stdin);             // স্ট্রিং ইনপুট নিচ্ছে (স্পেসসহ), সর্বোচ্চ ৫০ অক্ষর পর্যন্ত
                                     // ⚠️ এটি ইনপুটের শেষে নিউলাইন ক্যারেক্টার '\n' রেখে দেয়

    printf("%s", s);                 // ইনপুট স্ট্রিংটি প্রিন্ট করছে

    return 0;                        // প্রোগ্রাম সফলভাবে শেষ
}

/*
Input:
Abdullah Al Zubaer

Output:
Abdullah Al Zubaer

Note:
এই প্রোগ্রামে `fgets()` ব্যবহার করা হয়েছে যাতে ইউজার স্পেসসহ স্ট্রিং ইনপুট দিতে পারে 📝
⚠️ মনে রাখবে, `fgets()` ইনপুট শেষে একটি `\n` (newline) রেখে দেয়,
যা `printf()` করার সময় দেখা যায় — চাইলে সেটি `strtok(s, "\n")` দিয়ে রিমুভ করা যায় ✅
*/
