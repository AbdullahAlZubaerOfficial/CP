#include <stdio.h>              // 📚 স্ট্যান্ডার্ড ইন-আউটপুট হেডার ফাইল ইনক্লুড

int main()                       // 🏁 প্রোগ্রামের এন্ট্রি-পয়েন্ট
{
    int n;                       // 🔢 n ভ্যারিয়েবল—কতগুলি সংখ্যা ইনপুট নেবো
    scanf("%d", &n);             // ➡️ ইউজার থেকে n ইনপুট

    int ar[n];                   // 📦 n সাইজের Variable Length Array (VLA) ডিক্লেয়ার

    for (int i = 0; i < n; i++)  // 🔁 ০ থেকে n-১ পর্যন্ত লুপ—ইনপুট নেওয়ার জন্য
    {
        scanf("%d", &ar[i]);     //    ↳ প্রতিবার ar[i] তে একটি সংখ্যা সেভ করি
    }

    for (int i = n - 1; i >= 0; i--) // 🔁 রিভার্স লুপ—শেষ ইনডেক্স থেকে ০ পর্যন্ত
    {
        printf("%d ", ar[i]);        //    ↳ উল্টো ক্রমে প্রতিটি মান প্রিন্ট
    }

    return 0;                    // ✅ সফল সমাপ্তি
}

/*
Input:
5
10 20 30 40 50

Output:
50 40 30 20 10
*/
