#include <stdio.h> // Standard input-output library include

int main()
{
    int a[5]; // ৫টি সংখ্যার জন্য একটি integer array ডিক্লেয়ার করা হয়েছে

    // ইউজার থেকে একসাথে ৫টি সংখ্যা ইনপুট নেওয়া হচ্ছে
    scanf("%d %d %d %d %d", &a[0], &a[1], &a[2], &a[3], &a[4]);

    // সেই ৫টি সংখ্যা একসাথে প্রিন্ট করা হচ্ছে
    printf("%d %d %d %d %d", a[0], a[1], a[2], a[3], a[4]);

    return 0; // প্রোগ্রাম সফলভাবে শেষ
}


/*

📥 Input:
5 6 8 9 7

📤 Output:
5 6 8 9 7

📝 Note:
এই প্রোগ্রামটি ইউজার থেকে ৫টি সংখ্যা ইনপুট নেয় এবং তারপর সেগুলো আবার প্রিন্ট করে।

👉 এখানে প্রতিটি সংখ্যাকে আলাদা করে `a[0]` থেকে `a[4]` তে রাখা হয়েছে।

🎯 এটি array এর basic input-output প্র্যাকটিস করার জন্য খুব ভালো উদাহরণ ✨

💡 চাইলে তুমি loop ব্যবহার করে আরও সহজে করতে পারো ⬇️
```c
for(int i = 0; i < 5; i++) {
    scanf("%d", &a[i]);
}
for(int i = 0; i < 5; i++) {
    printf("%d ", a[i]);
}
    
*/