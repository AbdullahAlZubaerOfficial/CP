#include <stdio.h>      // ইনপুট-আউটপুট ফাংশনের হেডার ফাইল

int main()
{
    int n;              // n ভ্যারিয়েবল: অ্যারেতে কয়টি মান থাকবে
    scanf("%d", &n);    // ইউজার থেকে n ইনপুট নিচ্ছে

    int ar[n];          // n সাইজের একটি Variable Length Array (VLA) ডিক্লেয়ার

    // ইউজার থেকে nটি সংখ্যা ইনপুট নেওয়ার লুপ
    for (int i = 0; i < n; i++)
    {
        scanf("%d", &ar[i]);   // প্রতিবার ar[i] তে একটি সংখ্যা রাখা হচ্ছে
    }

    int idx, val;              // idx = যেখানে ইনসার্ট করবো, val = যে মান ইনসার্ট করবো
    scanf("%d %d", &idx, &val); // ইউজার থেকে idx ও val ইনপুট নিচ্ছে

    // ডানদিক থেকে বামে শিফট করে জায়গা খালি করছি
    for (int i = n; i >= idx + 1; i--)
    {
        ar[i] = ar[i - 1];     // প্রতিটি উপাদানকে এক ধাপ ডানে সরানো
    }

    ar[idx] = val;             // নির্দিষ্ট idx-এ নতুন মান বসানো

    // ইনসার্ট শেষে (নতুন সাইজ n+1) অ্যারে প্রিন্ট করা
    for (int i = 0; i <= n; i++)
    {
        printf("%d ", ar[i]);  // প্রতিটি উপাদান একে একে প্রিন্ট
    }

    return 0;                  // প্রোগ্রাম সফলভাবে শেষ
}

/*
📥 Input:
5
10 20 30 40 50
2 99

📤 Output:
10 20 99 30 40 50

📝 Note:
- প্রথম ইনপুট `5` → অ্যারেতে ৫টি এলিমেন্ট।
- পরের লাইন `10 20 30 40 50` → অ্যারেটির মান।
- শেষ ইনপুট `2 99` → index 2-এ মান 99 ইনসার্ট।  
  (index গণনা ০ থেকে শুরু — তাই ২ = তৃতীয় অবস্থান)

⚠️ এখানে `ar`-এর আকার আসল n (৫) হলেও আমরা index `n` অ্যাকসেস করছি,  
  যা VLA-তে টেকনিক্যালি “এক ধাপ” বেশি। production কোডে নিরাপদ হতে  
  `int ar[n + 1];` ডিক্লেয়ার করলে আউট-অফ-বাউন্ড রিস্ক থাকত না। 😉
*/
