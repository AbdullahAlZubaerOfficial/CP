#include <stdio.h> // Standard input-output লাইব্রেরি

int main()
{
    int a = 10; // a ভেরিয়েবলের মান 10
    int b = 20; // b ভেরিয়েবলের মান 20

    int tmp = a; // tmp নামের ভেরিয়েবলে a এর মান রাখা হলো, অর্থাৎ tmp = 10
    a = b;       // এখন a তে b এর মান বসানো হলো, এখন a = 20
    b = tmp;     // এখন b তে আগের a এর মান বসানো হলো, b = 10

    printf("a = %d\nb = %d", a, b); // নতুন মানগুলো প্রিন্ট করা হলো

    return 0; // প্রোগ্রাম সফলভাবে শেষ
}

/*

📤 Output:
a = 20
b = 10

📝 Note:
এই প্রোগ্রামটি দুটি ভেরিয়েবলের মান একে অপরের সঙ্গে অস্থায়ী `tmp` ভেরিয়েবল ব্যবহার করে অদলবদল (swap) করে।

📌 মূলধারাটি হলো:
- tmp = a;
- a = b;
- b = tmp;

🔁 এর ফলে a আর b এর মান একে অপরের সঙ্গে অদলবদল হয় ✅

*/
