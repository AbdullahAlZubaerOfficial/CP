#include <stdio.h>  // ইনপুট-আউটপুট ফাংশন ব্যবহারের জন্য হেডার ফাইল

int main()
{
    // 0 থেকে 4 পর্যন্ত লুপ চলবে (মোট ৫ বার)
    for (int i = 0; i <= 4; i++)
    {
        printf("ar[%d]\n", i);  // প্রতিবার ar[i] ফর্ম্যাটে প্রিন্ট করবে
    }

    return 0; // প্রোগ্রাম সফলভাবে শেষ
}


/*
📥 Input:
(কোনো ইনপুট লাগে না)

📤 Output:
ar[0]
ar[1]
ar[2]
ar[3]
ar[4]

📝 Note:
এই প্রোগ্রামটি ০ থেকে ৪ পর্যন্ত প্রতিটি সংখ্যাকে index এর মতো দেখিয়ে প্রিন্ট করে।
একটি array এর index গুলোর মত করে দেখানো হয়েছে।
যেমন: যদি একটি array হয় → `int ar[5];` তাহলে index গুলো হবে → 0 1 2 3 4

এটি সাধারণত loop দিয়ে array access শেখার জন্য ব্যবহৃত হয়।
*/
