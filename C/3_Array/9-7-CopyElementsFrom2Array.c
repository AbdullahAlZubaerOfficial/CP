#include <stdio.h> // ইনপুট-আউটপুট ব্যবহারের জন্য হেডার ফাইল 📥📤

int main()
{
    int n;                              // প্রথম অ্যারের সাইজ n 🧮
    scanf("%d", &n);                    // n ইনপুট নিচ্ছে ⌨️

    int a[n];                           // প্রথম অ্যারে ডিক্লেয়ার করা হল 🧊
    for (int i = 0; i < n; i++)         // n বার লুপ চালিয়ে ইনপুট নিচ্ছে 🔁
    {
        scanf("%d", &a[i]);             // a[i] ইনপুট নিচ্ছে
    }

    int m;                              // দ্বিতীয় অ্যারের সাইজ m 🧮
    scanf("%d", &m);                    // m ইনপুট নিচ্ছে ⌨️

    int b[m];                           // দ্বিতীয় অ্যারে ডিক্লেয়ার করা হল 🧊
    for (int i = 0; i < m; i++)         // m বার লুপ চালিয়ে ইনপুট নিচ্ছে 🔁
    {
        scanf("%d", &b[i]);             // b[i] ইনপুট নিচ্ছে
    }

    int c[n + m];                       // দুইটি অ্যারেকে মিলে নতুন অ্যারে c[n + m] তৈরি করলাম 🔗

    for (int i = 0; i < n; i++)         // প্রথম অ্যারের সব উপাদান c-তে কপি করছি 📝
    {
        c[i] = a[i];
    }

    for (int i = 0; i < m; i++)         // দ্বিতীয় অ্যারের সব উপাদান c-তে কপি করছি (n ইনডেক্স থেকে) ➕
    {
        c[i + n] = b[i];
    }

    for (int i = 0; i < n + m; i++)     // মিশ্রিত অ্যারে c প্রিন্ট করছি 📤
    {
        printf("%d ", c[i]);
    }

    return 0;                           // প্রোগ্রাম শেষ ✅
}

/*
Input:
5
1 4 5 8 9
4
1 5 8 9

Output:
1 4 5 8 9 1 5 8 9

🔎 Short Note:
• এই প্রোগ্রামটি দুটি অ্যারে `a` এবং `b` ইনপুট নিয়ে তাদের উপাদানগুলোকে
  একত্র করে নতুন অ্যারে `c` তে রাখে।
• প্রথমে `a` এর সব উপাদান `c` তে কপি করে, পরে `b` এর উপাদানগুলো `c` তে যুক্ত করে।
• শেষে পুরো `c` অ্যারেটি প্রিন্ট করে ➕📤
• এটি মূলত array merging-এর একটি সহজ উদাহরণ 🧩
*/
