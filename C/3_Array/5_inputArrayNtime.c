#include <stdio.h> // Standard input-output লাইব্রেরি include করা হয়েছে

int main()
{
    int n; // ইউজার কতটি সংখ্যা দিবে তা রাখার জন্য একটি ভেরিয়েবল n
    scanf("%d", &n); // ইউজার থেকে n এর ইনপুট নেওয়া হচ্ছে

    int ar[n]; // n সাইজের একটি array ডিক্লেয়ার করা হয়েছে (VLA - Variable Length Array)

    // ইউজার থেকে n সংখ্যক ইনপুট নেওয়ার জন্য লুপ
    for (int i = 0; i < n; i++)
    {
        scanf("%d", &ar[i]); // প্রতিবার ar[i] তে একটি সংখ্যা রাখা হচ্ছে
    }

    // ইনপুট নেওয়া সংখ্যা গুলো আবার প্রিন্ট করার জন্য আরেকটি লুপ
    for (int i = 0; i < n; i++)
    {
        printf("%d ", ar[i]); // প্রতিবার ar[i] প্রিন্ট করা হচ্ছে
    }

    return 0; // প্রোগ্রাম সফলভাবে শেষ
}


/*
📥 Input:
5
10 20 30 40 50

📤 Output:
10 20 30 40 50

📝 Note:
এই প্রোগ্রামটি ইউজার থেকে একটি সংখ্যা n ইনপুট নেয় — যেটা array-এর সাইজ নির্ধারণ করে।
তারপর ইউজার n সংখ্যক ইনপুট দেয় এবং প্রোগ্রামটি সেগুলো ঠিক একইভাবে প্রিন্ট করে দেয়। 🔁

⭐ এখানে VLA (Variable Length Array) ব্যবহার করা হয়েছে,
যার কারণে `int ar[n];` ডিক্লেয়ার করা সম্ভব হয়েছে (C99 বা এর পরের ভার্সনে এটা কাজ করে)। 📌

💡 তুমি চাইলে future-এ loop দিয়ে ইনপুট প্রিন্ট করার সময় reverse করেও প্রিন্ট করতে পারো। চাইলে সেই কোডটাও করে দিবো! 😎
*/
