#include <stdio.h>   // 🌟 Standard input/output header

int main() {
    int n;
    scanf("%d", &n);                // 📥 ইউজার থেকে সংখ্যা ইনপুট

    /* 🍀 সব সম্ভাব্য “লাকি” সংখ্যা যেগুলোকে problem-এ divisor হিসেবে ধরা হয় */
    int lucky[] = {4, 7, 44, 47, 74, 77,
                   444, 447, 474, 477, 744, 747, 774, 777};

    int size = 14;                  // 🔢 lucky[] অ্যারেতে মোট ১৪টি উপাদান

    /* 🔁 প্রতিটি lucky সংখ্যার সাথে n ভাগ করে দেখি পুরোপুরি ভাগ যায় কি-না  */
    for (int i = 0; i < size; i++) {
        if (n % lucky[i] == 0) {    // ✅ যদি n ⚡ lucky[i] দিয়ে নিঃশেষ ভাগ হয়
            printf("YES\n");        //    ➜ n “almost lucky” (divisible by lucky)
            return 0;               // 💨 খুঁজে পেয়েছি, প্রোগ্রাম এখানেই শেষ
        }
    }

    printf("NO\n");                 // ❌ কোনও lucky divisor পাওয়া যায়নি
    return 0;                       // ✅ সফল সমাপ্তি
}

/* =====================================================
📥 Input:
47

📤 Output:
YES

🧠 Short Note:
এই প্রোগ্রামটি যাচাই করে প্রদত্ত সংখ্যা n কি 4 ও 7-এর দ্বারা
গঠিত “lucky number”-গুলোর (4, 7, 44, …, 777) কোনটির দ্বারাই
পুরোপুরি ভাগ যায় কি-না। ভাগ গেলে "YES", না গিলে "NO" প্রিন্ট করে।

⏱ Time Complexity (TC):  O(14) ≈ O(1)
   ▸ সর্বদা মাত্র ১৪ বার modulus চেক—স্থায়ী সময়।

💾 Space Complexity (SC): O(1)
   ▸ অতিরিক্ত শুধু ১৪-ঘরের int অ্যারে ও কয়েকটি ভ্যারিয়েবল।
===================================================== */
