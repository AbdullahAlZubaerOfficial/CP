#include <stdio.h>      // 🌟 Standard I/O library
#include <limits.h>     // 🌟 INT_MIN constant use করার জন্য

int main() { 
    int n;
    scanf("%d", &n);    // 📥 ইউজার থেকে ইনপুট: অ্যারের সাইজ

    int ar[n];          // 🔢 n সাইজের অ্যারে ডিক্লেয়ার

    for(int i = 0; i < n; i++) {
        scanf("%d", &ar[i]);    // 📥 n সংখ্যক ইনপুট নেওয়া হচ্ছে
    }

    int max = INT_MIN;  // 🔍 সর্বনিম্ন মান দিয়ে max ইনিশিয়ালাইজ

    for(int i = 0; i < n; i++) {
        if(ar[i] > max) {
            max = ar[i];        // ✅ যদি বর্তমান সংখ্যা বড় হয়, সেটাই max
        }
    }

    printf("%d", max);  // 📤 সর্বোচ্চ মান প্রিন্ট করা হচ্ছে
    
    return 0;
}

/* =====================================================
📥 Input:
6
-5 3 99 -2 0 42

📤 Output:
99

🧠 Short Note:
এই প্রোগ্রামটি একটি অ্যারেতে থাকা সব সংখ্যার মধ্যে সবচেয়ে বড় সংখ্যাটি
(n সংখ্যার মধ্যে maximum element) বের করে প্রিন্ট করে।

⏱ Time Complexity (TC):  O(n)
   ▸ প্রতিটি উপাদান একবার করে চেক করা হয়।

💾 Space Complexity (SC): O(n)
   ▸ একটি n সাইজের অ্যারে ব্যবহার হয়েছে।

📌 Tip:
INT_MIN ব্যবহার করলে যেকোনো ইনপুটের ক্ষেত্রেই প্রোগ্রাম সঠিকভাবে কাজ করবে,
যেমন ঋণাত্মক সংখ্যাও ইনপুট দিলে ঠিকভাবে সর্বোচ্চ মান বের করতে পারবে।
===================================================== */
