#include <stdio.h>         // ইনপুট-আউটপুট ফাংশনের জন্য হেডার ফাইল
#include <string.h>        // স্ট্রিং ফাংশনের জন্য হেডার ফাইল

int main()
{
    char a[101], b[101];         // দুটি স্ট্রিং ইনপুট রাখার জন্য অ্যারে

    scanf("%s %s", &a, &b);      // ইউজার থেকে দুটি স্ট্রিং ইনপুট নিচ্ছে (স্পেস ছাড়া)

    strcpy(a, b);                // b স্ট্রিংটি a তে কপি করে নিচ্ছে

    printf("%s %s", a, b);       // কপি করার পর দুটি স্ট্রিংই প্রিন্ট করছে

    return 0;                    // প্রোগ্রাম সফলভাবে শেষ
}

/*
Input:
abcd
jklm

Output:
jklm jklm

Note:
এই প্রোগ্রামে `strcpy(a, b)` ব্যবহারে `b` এর মান `a` তে কপি হয়ে যায় 🧠  
📌 তাই প্রিন্ট করার সময় দুইবার `jklm` দেখা যায় কারণ `a` এখন `b` এর সমান হয়ে গেছে।  
⚠️ `scanf("%s %s", ...)` স্পেস ছাড়া স্ট্রিং নেয়, তাই স্পেস দেওয়া ইনপুট নিলে শুধু প্রথম শব্দটি ইনপুট হবে।

*/
