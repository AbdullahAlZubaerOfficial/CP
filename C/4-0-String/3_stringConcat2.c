#include <stdio.h>         // ইনপুট-আউটপুট ফাংশনের জন্য হেডার ফাইল
#include <string.h>        // স্ট্রিং ম্যানিপুলেশন ফাংশনের জন্য হেডার ফাইল

int main()
{
    char a[101], b[101];           // দুটি স্ট্রিং ইনপুট রাখার জন্য অ্যারে

    scanf("%s %s", &a, &b);        // দুটি স্ট্রিং ইনপুট নিচ্ছে (স্পেস ছাড়া)

    strcat(a, b);                  // b স্ট্রিংটিকে a স্ট্রিংয়ের শেষে জুড়ে দিচ্ছে

    printf("%s %s", a, b);         // প্রথমে জোড়া দেওয়া স্ট্রিং (a), তারপর আলাদা b প্রিন্ট করছে

    return 0;                      // প্রোগ্রাম সফলভাবে শেষ
}

/*
Input:
hello world

Output:
helloworld world

Note:
🔹 `strcat(a, b)` এর মাধ্যমে `b` এর মান `a` এর শেষে যোগ হয়ে যায়।  
🔹 ফলে `a` হয়ে যায়: "hello" + "world" = "helloworld"  
🔹 `b` অপরিবর্তিত থাকে।  
⚠️ `scanf("%s %s", ...)` ইনপুটে স্পেস দিলেই দুটি আলাদা স্ট্রিং ইনপুট নেয়।  
📌 `strcat()` ব্যবহার করার সময় মনে রাখবে `a` তে যথেষ্ট জায়গা থাকতে হবে যেন `b` এর ক্যারেক্টারগুলো যোগ করা যায়।

*/
