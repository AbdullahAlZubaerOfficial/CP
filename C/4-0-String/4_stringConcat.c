#include <stdio.h>          // ইনপুট-আউটপুট ফাংশনের হেডার ফাইল 😊
#include <string.h>         // strlen() ব্যবহারের জন্য হেডার ফাইল

int main()
{
    char a[101], b[101];          // দুইটি স্ট্রিং ইনপুটের জন্য অ্যারে (সর্বোচ্চ ১০০ অক্ষর + null)

    scanf("%s %s", a, b);         // দুইটি শব্দ ইনপুট নিচ্ছি (স্পেস দিলে আলাদা ধরে)

    int length_a = strlen(a);     // a-এর দৈর্ঘ্য বের করছি
    int length_b = strlen(b);     // b-এর দৈর্ঘ্য বের করছি

    /* ম্যানুয়ালি b এর সব ক্যারেক্টার a-এর শেষে কপি করছি  */
    for (int i = 0; i <= length_b; i++)   // '<=' রাখলাম যেন শেষের '\0'-ও কপি হয়
    {
        a[i + length_a] = b[i];           // a-এর শেষে b[i] বসাচ্ছি
    }

    printf("%s %s", a, b);         // প্রথমে জোড়া দেওয়া স্ট্রিং, তারপর b আলাদা করে প্রিন্ট

    return 0;                      // প্রোগ্রাম সফল সমাপ্তি
}

/*
Input:
abcd jklm

Output:
abcdjklm jklm

Note:
🔹 এখানে আমরা `strlen()` দিয়ে দৈর্ঘ্য জেনে, লুপে করে `b`-এর সব ক্যারেক্টার `a`-এর শেষে জুড়ে দিয়েছি  
🔹 শেষের `\0` কপি করায় `a` এখন বৈধ null-terminated স্ট্রিং ✨  
🔹 চাইলে অল্পকোডে একই কাজ `strcat(a, b)` দিয়ে আরও সহজে করা যেত 😉
*/
