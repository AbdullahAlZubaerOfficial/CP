#include <stdio.h>
#include <string.h>

int main()
{
    int n;
    scanf("%d", &n); // মোট কতটি টেস্ট কেস ইনপুট নেবে

    while (n--)
    {
        char s[51], t[51], result[101]; // s এবং t এর ম্যাক্সিমাম দৈর্ঘ্য ৫০, রেজাল্ট ১০০ পর্যন্ত হতে পারে
        scanf("%s %s", s, t); // দুইটি স্ট্রিং ইনপুট নেয়

        int lenS = strlen(s); // s এর দৈর্ঘ্য
        int lenT = strlen(t); // t এর দৈর্ঘ্য
        int i = 0, j = 0, k = 0; // ইন্ডেক্স কাউন্টার

        // s এবং t এর ক্যারেক্টার গুলো交互 (alternately) মিশিয়ে নেয়া হচ্ছে
        while (i < lenS && j < lenT)
        {
            result[k++] = s[i++]; // s থেকে একটি ক্যারেক্টার নেয়া হচ্ছে
            result[k++] = t[j++]; // t থেকে একটি ক্যারেক্টার নেয়া হচ্ছে
        }

        // s এ যদি অতিরিক্ত ক্যারেক্টার থেকে যায় তাহলে সেগুলো যোগ করা হচ্ছে
        while (i < lenS)
        {
            result[k++] = s[i++];
        }

        // t তেও যদি অতিরিক্ত ক্যারেক্টার থাকে তাহলে সেগুলো যোগ করা হচ্ছে
        while (j < lenT)
        {
            result[k++] = t[j++];
        }

        result[k] = '\0'; // স্ট্রিং এর শেষ

        printf("%s\n", result); // ফলাফল প্রিন্ট
    }

    return 0; // প্রোগ্রাম সফলভাবে শেষ
}

/*

Input:
2
abc def
abcd xy

Output:
adbcef
axbycd

📝 নোট:
- এই প্রোগ্রাম দুইটি স্ট্রিং কে交互ভাবে (alternately) মিলিয়ে দেয়।
- যদি কোন স্ট্রিং ছোট হয়, তাহলে বড় স্ট্রিং এর বাকি অংশ যোগ হবে।
- বেশ সহজ ও সুন্দর একটা মিক্সিং লজিক।

*/
