#include <stdio.h>

int main()
{
    int a, b, c, d;                          // চারটি পূর্ণসংখ্যা ইনপুট নেওয়ার জন্য ভ্যারিয়েবল
    scanf("%d %d %d %d", &a, &b, &c, &d);  // ইউজার থেকে চারটি সংখ্যা ইনপুট নেওয়া

    // ধাপে ধাপে প্রত্যেক সংখ্যার শেষ দুই ডিজিট নিয়ে গুণ করা হচ্ছে এবং ১০০ দিয়ে মড নেওয়া হচ্ছে
    int result = a % 100;                    // a এর শেষ দুই ডিজিট

    result = (result * (b % 100)) % 100;    // (a_last_two * b_last_two) % 100
    result = (result * (c % 100)) % 100;    // উপরের ফলাফল * c_last_two % 100
    result = (result * (d % 100)) % 100;    // উপরের ফলাফল * d_last_two % 100

    // দুই ডিজিট সবসময় দেখানোর জন্য ফরম্যাটিং (যেমন 08, 09)
    printf("%02d\n", result);

    return 0;
}

/*

Input:
123 456 789 10

Output:
80

📝 Short Note:
- এখানে চারটি সংখ্যার শেষ দুই ডিজিট নিয়ে তাদের গুণফলকে ১০০ দিয়ে ভাগ করার রিমাইন্ডার বের করা হয়েছে।
- printf এ %02d ব্যবহার করা হয়েছে যাতে ফলাফল দুই ডিজিট হিসেবে দেখায়, যেমন 08 বা 09।
- এটি last two digits কে ধরে রাখার জন্য modular arithmetic ব্যবহার করেছে।

*/
