#include <stdio.h> // ইনপুট ও আউটপুটের জন্য হেডার ফাইল ইনক্লুড করছি

int main() // মেইন ফাংশন শুরু
{
    char s[101]; // ইনপুট স্ট্রিং রাখার জন্য ১০১ সাইজের ক্যারেক্টার অ্যারে নিচ্ছি

    scanf("%s", s); // ইউজার থেকে স্ট্রিং ইনপুট নিচ্ছি (word বা sentence নয়, শুধু একটা শব্দ)

    int freq[26] = {0}; // ২৬টি ছোট হাতের অক্ষরের frequency (ঘনত্ব) রাখার জন্য একটা অ্যারে নিচ্ছি এবং সব মান ০ দিয়ে initialize করছি

    // স্ট্রিং এর প্রতিটি অক্ষর নিয়ে লুপ চালাচ্ছি
    for (int i = 0; s[i] != '\0'; i++) // যতক্ষণ না null character পাওয়া যায় (স্ট্রিং শেষ)
    {
        if (s[i] >= 'a' && s[i] <= 'z') // যদি অক্ষরটি ছোট হাতের হয়
        {
            freq[s[i] - 'a']++; // অক্ষরের ASCII মান থেকে 'a' বাদ দিলে ইনডেক্স পাওয়া যায়, ওই ইনডেক্সে ১ বাড়িয়ে দিলাম
        }
    }

    // এখন frequency অ্যারে চেক করে যেগুলোর মান ০ থেকে বেশি, তাদের প্রিন্ট করব
    for (int i = 0; i < 26; i++) // ০ থেকে ২৫ পর্যন্ত ঘুরে ঘুরে যাচ্ছি
    {
        if (freq[i] > 0) // যদি কোন অক্ষর স্ট্রিংয়ে পাওয়া যায় (ঘনত্ব > 0)
        {
            printf("%c : %d\n", i + 'a', freq[i]); // ইনডেক্স থেকে অক্ষর বানাচ্ছি (i + 'a') আর frequency প্রিন্ট করছি
        }
    }

    return 0; // প্রোগ্রাম শেষ
}

/*
📥 Input:
applebanana

📤 Output:
a : 4
b : 1
e : 1
l : 1
n : 2
p : 2

📝 Short Note:
👉 এই প্রোগ্রামটি একটি lowercase স্ট্রিং ইনপুট নেয় এবং প্রতিটি ছোট হাতের অক্ষর কতবার এসেছে তা প্রিন্ট করে।

✅ ASCII Code ব্যবহার করে 'a' থেকে 'z' পর্যন্ত অক্ষরগুলোর অবস্থান বের করে count করা হয়।

🎯 উদাহরণ:  
    'a' - 'a' = 0  
    'b' - 'a' = 1  
    ...  
    'z' - 'a' = 25  

এই ইনডেক্সে প্রতিটি অক্ষরের জন্য কাউন্ট রাখা হয় এবং পরে তা প্রিন্ট করা হয়।
*/

