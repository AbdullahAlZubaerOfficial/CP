#include <stdio.h>       // ইনপুট-আউটপুট ফাংশনের জন্য হেডার ফাইল

int main()
{
    char s[1000001];          // একটি বড় স্ট্রিং ইনপুট রাখার জন্য অ্যারে

    fgets(s, 1000001, stdin); // ইউজার ইনপুট নিচ্ছে (স্পেস সহ), সর্বোচ্চ ১০০০০০০ ক্যারেক্টার পর্যন্ত

    for (int i = 0; s[i] != '\\'; i++)  // যতক্ষণ পর্যন্ত \ পাই না, ততক্ষণ ক্যারেক্টার প্রিন্ট করছে
    {
        printf("%c", s[i]);             // ক্যারেক্টার বাই ক্যারেক্টার প্রিন্ট করছে
    }

    return 0;                           // প্রোগ্রাম সফলভাবে শেষ
}

/*
Input:
abdullah \al zubaer

Output:
abdullah

Note:
এই প্রোগ্রামটি ইউজার থেকে একটি স্ট্রিং ইনপুট নেয় এবং ব্যাকস্ল্যাশ (`\`) আসার আগ পর্যন্ত সব ক্যারেক্টার প্রিন্ট করে ✨  
📌 যেমন: "abdullah \al zubaer" ইনপুট দিলে শুধু "abdullah" পর্যন্ত প্রিন্ট হবে।
⚠️ মনে রাখবে, ব্যাকস্ল্যাশ `\` একটি escape character, তাই ইনপুটে সঠিকভাবে ব্যবহার করতে হলে ডাবল ব্যাকস্ল্যাশ (`\\`) প্রিন্ট করতে হয়।
*/
