#include <stdio.h>

// 📌 ফাংশন: একটি অ্যারেতে কয়টি জোড় (even) ও বিজোড় (odd) সংখ্যা আছে তা গণনা ও প্রিন্ট করে
void odd_even(){
    int n;
    scanf("%d", &n); // ইউজার থেকে অ্যারের সাইজ ইনপুট নিচ্ছে

    int ar[n]; // সাইজ অনুযায়ী অ্যারে ডিক্লেয়ার করা হলো

    for(int i = 0; i < n; i++){
        scanf("%d", &ar[i]); // অ্যারের প্রতিটি উপাদান ইনপুট নিচ্ছে
    }

    int even = 0, odd = 0; // জোড় ও বিজোড় কাউন্টার শুরু

    for(int i = 0; i < n; i++){
        if(ar[i] % 2 == 0){ // যদি সংখ্যাটি ২ দিয়ে নিঃশেষে ভাগ যায়, তবে even
            even++;
        } else { // নাহলে odd
            odd++;
        }
    }

    printf("%d %d", even, odd); // জোড় ও বিজোড় সংখ্যার পরিমাণ প্রিন্ট করা হলো
}

int main() { 
    odd_even(); // ফাংশন কল করা হলো
    return 0;
}

/*
📥 Input:
5
2 3 6 7 8

📤 Output:
3 2

📝 Short Note:
এই প্রোগ্রামটি একটি অ্যারে ইনপুট নিয়ে গণনা করে যে, কতটি সংখ্যা জোড় এবং কতটি সংখ্যা বিজোড়।
উপরের ইনপুটে: 2, 6, 8 → Even (3টি) এবং 3, 7 → Odd (2টি)
তাই আউটপুট: 3 2

✅ Time Complexity: O(n)
✅ Space Complexity: O(n)

🎯 এই কোডটা একদম beginner-friendly — সহজ লজিকে গঠিত এবং ফাংশন ইউজ করে লেখা হয়েছে যাতে ভবিষ্যতে পুনঃব্যবহার করা যায়।
*/
