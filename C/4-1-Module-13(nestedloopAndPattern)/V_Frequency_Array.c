#include <stdio.h>

int main() { 
    int n, m;
    scanf("%d %d", &n, &m); // 🔢 n = অ্যারের সাইজ, m = সর্বোচ্চ সম্ভাব্য সংখ্যা

    int ar[n];             // ইনপুট সংখ্যা রাখার অ্যারে
    int freq[m + 1];       // ফ্রিকোয়েন্সি কাউন্ট রাখার অ্যারে (1 থেকে m পর্যন্ত)

    // ফ্রিকোয়েন্সি অ্যারে শূন্য দিয়ে ইনিশিয়ালাইজ
    for(int i = 0; i <= m; i++) {
        freq[i] = 0;
    }

    // ইউজার ইনপুট নিচ্ছে
    for(int i = 0; i < n; i++) {
        scanf("%d", &ar[i]);
    }

    // প্রতিটি সংখ্যার ফ্রিকোয়েন্সি কাউন্ট করছে
    for(int i = 0; i < n; i++) {
        freq[ar[i]]++;
    }

    // 1 থেকে m পর্যন্ত প্রতিটি সংখ্যার ফ্রিকোয়েন্সি প্রিন্ট করছে
    for(int i = 1; i <= m; i++) {
        printf("%d\n", freq[i]);
    }

    return 0;
}


/*
📥 Input:
8 5
1 1 3 5 5 5 2 3

📤 Output:
2
1
2
0
3

📝 Short Note:
👉 এই প্রোগ্রামটি 1 থেকে m পর্যন্ত সংখ্যাগুলোর ফ্রিকোয়েন্সি কাউন্ট করে।
👉 প্রথমে `freq` অ্যারেটি 0 দিয়ে ইনিশিয়ালাইজ করা হয়।
👉 তারপর ইনপুট অ্যারের প্রতিটি সংখ্যার জন্য `freq[number]++` করে গোনা হয়।
👉 শেষে 1 থেকে m পর্যন্ত প্রতিটি সংখ্যার গননা প্রিন্ট করা হয়।

⚙️ Time Complexity: O(n + m)
📦 Space Complexity: O(m)
*/

