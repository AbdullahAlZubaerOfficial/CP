#include <stdio.h>   // ইনপুট-আউটপুটের জন্য প্রয়োজনীয় হেডার ফাইল

int main() { 
    int n;
    scanf("%d", &n);  // ইনপুট: কতটি সংখ্যা থাকবে অ্যারেতে

    int ar[n];        // n সাইজের অ্যারে ডিক্লেয়ার

    for (int i = 0; i < n; i++) {
        scanf("%d", &ar[i]);  // অ্যারের প্রতিটি উপাদান ইনপুট নিচ্ছি
    }

    int x;
    scanf("%d", &x);  // ইনপুট: কোন যোগফল খুঁজবো

    int flag = 0;     // শুরুতে ধরে নিচ্ছি জোড়া মেলেনি

    // প্রতিটি জোড়া উপাদান পরীক্ষা করে দেখবো যোগফল x হয় কিনা
    for (int i = 0; i < n; i++) {
        for (int j = i + 1; j < n; j++) {
            if (ar[i] + ar[j] == x) {
                flag = 1;  // একবারও মিলে গেলে ফ্ল্যাগ 1 করে দিবো
            }
        }
    }

    if (flag == 1) {
        printf("yes\n");  // যদি জোড়া মেলে, "yes"
    } else {
        printf("no\n");   // না মেললে, "no"
    }

    return 0;  // প্রোগ্রাম সফলভাবে শেষ
}


/*
📥 Input:
5
1 2 3 4 5
7

📤 Output:
yes

📥 Input:
4
1 2 3 9
10

📤 Output:
no

📝 Short Note:
- এই প্রোগ্রামটি ব্যবহার করে আমরা যাচাই করি কোনো দুই উপাদানের যোগফল `x` হয় কিনা।
- `flag` ব্যবহার করে শেষে যাচাই করা হয় কোনো মিল পাওয়া গেছে কি না।
- এটি একটি ব্রুট ফোর্স সমাধান যার সময় জটিলতা O(n²)।
- একাধিক বার মিল পেলেও শুধু শেষ পর্যন্ত যাচাই করে, একবার `yes` বলবে কিন্তু লুপ শেষ করবে না।
- চাইলে efficiency বাড়াতে একবার পাওয়া মাত্রই `return 0;` দিয়ে প্রোগ্রাম থামানো যেত।
*/
