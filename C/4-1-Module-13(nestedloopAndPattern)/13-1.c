#include <stdio.h>   // ইনপুট-আউটপুটের জন্য হেডার ফাইল 😊

int main() {         // প্রোগ্রামের শুরু - main ফাংশন
    int n;           // মোট কত লাইন তারকা ছাপাবো, তা রাখার ভ্যারিয়েবল
    scanf("%d", &n); // ইউজার থেকে n ইনপুট নিচ্ছি

    int star = 1;    // প্রথম লাইনে কতটি * প্রিন্ট হবে (শুরুতে 1)

    for (int i = 1; i <= n; i++) {        // লাইন-সংখ্যা অনুযায়ী looping
        for (int j = 1; j <= star; j++) { // প্রতি লাইনে যতগুলো * লাগবে, তার লুপ
            printf("*");                  // তারকা ছাপাচ্ছি
        }
        printf("\n"); // এক লাইন শেষ, নতুন লাইনে যাচ্ছি
        star++;       // পরের লাইনে তারকার সংখ্যা ১ বাড়ালাম
    }

    return 0; // প্রোগ্রাম সফলভাবে শেষ ✅
}

/*
📥 Input:
5

📤 Output:
*
**
***
****
*****

🔍 Short Note:
এই প্রোগ্রামটি n-লাইনবিশিষ্ট একখানা বাম-দিকে বেজড (left-aligned) তারকা ত্রিভুজ 🌟 আঁকে।
বাইরের for-লুপ লাইন ধরে, আর ভেতরের for-লুপ সেই লাইনে কয়টা * প্রিন্ট হবে তা ঠিক করে।
প্রতিবার লাইনের শেষে \n দিয়ে পরের লাইনে গিয়ে star কাউন্টার ১ বাড়িয়ে দেয়, যাতে প্যাটার্ন ধাপে ধাপে বাড়ে। 😊
*/
