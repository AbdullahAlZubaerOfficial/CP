#include <stdio.h>   // ইনপুট-আউটপুটের ফাংশনের জন্য স্ট্যান্ডার্ড হেডার

int main() { 
    int n;
    scanf("%d", &n);          // ⭐️ ডায়মন্ড প্যাটার্নের উচ্চতা (n) ইনপুট নিচ্ছে

    int space = n - 1;        // প্রথম (উপরের) লাইনে বাম পাশে n-1 টা স্পেস থাকবে
    int star  = 1;            // প্রথম (উপরের) লাইনে ১টা চিহ্ন (# বা -) থাকবে

    /* 🔼 ওপরের অর্ধেক (মাঝের লাইনসহ) প্রিন্ট */
    for (int i = 1; i <= n; i++) {

        for (int j = 1; j <= space; j++) {   // প্রয়োজনীয় স্পেস প্রিন্ট
            printf(" ");
        }

        char ch = (i % 2 == 0) ? '-' : '#';  // জোড় লাইন → '-',  বিজোড় লাইন → '#'

        for (int j = 1; j <= star; j++) {    // লাইন অনুসারে নির্দিষ্ট সংখ্যক চিহ্ন প্রিন্ট
            printf("%c", ch);
        }

        printf("\n");        // লাইন শেষ, নতুন লাইনে যাই
        space--;             // পরের লাইনে স্পেস ১টি কমে
        star += 2;           // এবং চিহ্ন ২টি বেড়ে যায় (১→৩→৫ …)
    }

    /* 🔽 নিচের অর্ধেক (মাঝের লাইন বাদে) প্রিন্ট */
    space = 1;        // নিচের অংশের প্রথম লাইনে ১টা স্পেস লাগবে
    star  -= 4;       // মাঝের পরে নিচে নামার সময় ২ ধাপ কমিয়ে শুরু (… 5 → 3)

    for (int i = n - 1; i >= 1; i--) {

        for (int j = 1; j <= space; j++) {   // স্পেস প্রিন্ট
            printf(" ");
        }

        char ch = (i % 2 == 0) ? '-' : '#';  // একই নিয়মে চিহ্ন ঠিক করছি

        for (int j = 1; j <= star; j++) {    // তার পর সেই চিহ্নগুলো প্রিন্ট
            printf("%c", ch);
        }

        printf("\n");        // লাইন শেষ, নতুন লাইনে যাই
        space++;             // নীচে যাওয়ার সঙ্গে সঙ্গে স্পেস ১টি বাড়ে
        star  -= 2;          // এবং চিহ্ন ২টি কমে (… 3 → 1)
    }

    return 0;   // প্রোগ্রাম সফলভাবে শেষ
}

/*
📥 Input:
5

📤 Output:
    #
   ---
  #####
 -------
#########
 -------
  #####
   ---
    #

📝 Short Note:
- এই প্রোগ্রামটি একটি **ডায়মন্ড প্যাটার্ন** আঁকে।
- odd (বিজোড়) লাইনে `#` ও even (জোড়) লাইনে `-` ব্যবহার করা হয়েছে।
- ওপরের অর্ধে তারকা-সংখ্যা ১-৩-৫ … করে বাড়ে, তারপর নিচের অর্ধে  …৫-৩-১ করে কমে।
- টাইম কমপ্লেক্সিটি O(n²) এবং স্পেস কমপ্লেক্সিটি O(1) — শুধু কাউন্টার ভ্যারিয়েবল ব্যবহার হয়েছে।  
*/
