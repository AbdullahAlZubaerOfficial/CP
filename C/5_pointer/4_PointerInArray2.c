#include <stdio.h>

int main() { 
   int a[5] = {10, 20, 30, 40, 50};
   
   *(a) = 100; // *(a+1) দিলে index 1-এর মান পরিবর্তন হতো, *(a+2) দিলে index 2-এর মান পরিবর্তন হতো...

   for(int i = 0; i < 5; i++) {
       printf("%d ", a[i]);   // Output: 100 20 30 40 50
   }
   
   return 0;
}

/*
📥 Input:
(কোনো ইনপুট নেই)

📤 Output:
100 20 30 40 50

📝 Short Note:
👉 `a` একটি array যার মধ্যে 5টি উপাদান আছে: {10, 20, 30, 40, 50}
👉 `a` মূলত `&a[0]` এর সমতুল্য, অর্থাৎ প্রথম উপাদানের অ্যাড্রেস।
👉 `*(a)` মানে হচ্ছে `a[0]` — অর্থাৎ প্রথম উপাদানে গিয়ে মান বসানো হচ্ছে।
👉 তাই `*(a) = 100` এই লাইনটি প্রথম উপাদানকে 100 করে দিচ্ছে।

📌 যদি লিখতাম `*(a + 2) = 300;` তাহলে array এমন হতো: {10, 20, 300, 40, 50}

⚙️ Time Complexity: O(n)  
📦 Space Complexity: O(1)
*/

