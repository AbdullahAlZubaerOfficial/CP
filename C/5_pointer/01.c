#include <stdio.h>

int main() { 
   int x = 10;
   
   printf("%d\n", x);   // 10 → ভ্যালু প্রিন্ট করছে

   printf("%p", &x);    // মেমোরি অ্যাড্রেস প্রিন্ট করছে
   
   return 0;
}


/*
📥 Input:
(কোনো ইনপুট নেই)

📤 Output:
10
0x... (যেমন: 0x61ff1c — এটা প্রতিবার রান করলে ভিন্ন হতে পারে)

📝 Short Note:
👉 এই প্রোগ্রামটি প্রথমে একটি ইন্টিজার ভ্যারিয়েবল `x` ডিক্লেয়ার করে যার মান 10।
👉 `printf("%d", x);` এটি x-এর ভ্যালু (10) প্রিন্ট করে।
👉 `printf("%p", &x);` এটি x-এর মেমোরি অ্যাড্রেস প্রিন্ট করে, যেখানে `x` সংরক্ষিত আছে।

🔍 `%p` হল pointer format specifier যা মেমোরি অ্যাড্রেস দেখানোর জন্য ব্যবহৃত হয়।

🧠 এটি পয়েন্টার বা মেমোরি সম্পর্কিত ধারণা বোঝার জন্য খুবই গুরুত্বপূর্ণ।

⚙️ Time Complexity: O(1)
📦 Space Complexity: O(1)
*/
