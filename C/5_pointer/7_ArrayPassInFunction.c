#include <stdio.h> // স্ট্যান্ডার্ড ইনপুট আউটপুট হেডার ফাইল ইনক্লুড করা হয়েছে

// ফাংশন ডিফাইন করা হয়েছে যেটা একটি অ্যারে এবং অ্যারেটির সাইজ ইনপুট হিসেবে নেয়
void fun(int a[], int n) {
    for(int i = 0; i < n; i++) {         // অ্যারেটির প্রতিটি উপাদানে লুপ চালানো হচ্ছে
        printf("%d ", a[i]);             // প্রতিটি উপাদান প্রিন্ট করা হচ্ছে
    }
}

int main() { 
    int n;                               // একটি ইন্টিজার ভেরিয়েবল n ডিক্লেয়ার করা হলো
    scanf("%d", &n);                     // ইউজার থেকে n ইনপুট নেয়া হচ্ছে

    int a[n];                            // সাইজ n এর একটি অ্যারে তৈরি করা হলো

    for(int i = 0; i < n; i++) {         // অ্যারেটির প্রতিটি ইন্ডেক্সে লুপ চালানো হচ্ছে
        scanf("%d", &a[i]);              // ইউজার থেকে প্রতিটি ইনপুট অ্যারেতে রাখা হচ্ছে
    }

    fun(a, n);                           // ফাংশন কল করা হচ্ছে এবং অ্যারে ও সাইজ পাঠানো হচ্ছে

    return 0;                            // প্রোগ্রাম সফলভাবে শেষ করার জন্য return 0
}


/*
📥 Input:
5
10 20 30 40 50

📤 Output:
10 20 30 40 50

📝 সংক্ষিপ্ত ব্যাখ্যা:
👉 `fun(a, n)` ফাংশনের মাধ্যমে মূল অ্যারেটির রেফারেন্স পাঠানো হয়েছে।
👉 C তে ফাংশনে অ্যারে পাঠানো মানে মূল অ্যারেটিকে রেফারেন্স হিসেবে পাঠানো।
👉 তাই আমরা ফাংশন থেকে ডেটা এক্সেস করতে পারি, কিন্তু অ্যারে return করতে পারি না।

📌 এটি call by reference এর বাস্তব প্রয়োগ।

⚙️ Time Complexity: O(n)  
📦 Space Complexity: O(1)
*/
