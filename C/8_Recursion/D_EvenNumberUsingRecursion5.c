#include <stdio.h>  // ✅ standard input-output library

// 🔁 even() → 1 থেকে 100 এর মধ্যে যতগুলো even সংখ্যা আছে, সেগুলো প্রিন্ট করে
void even(int i) {

    if (i == 101) {     // ⛔ base case → 100 পর্যন্ত গিয়ে থেমে যাবে
        return;
    }

    if (i % 2 == 0) {   // ✅ even হলে
        printf("%d\n", i);  // প্রিন্ট করো সংখ্যা
    }

    even(i + 1);        // 🔁 প্রতি বার i বাড়িয়ে আবার ফাংশন কল
}

int main() {
    int i = 1;          // 🔢 শুরু i=1 থেকে
    even(i);            // 📞 even ফাংশন কল

    return 0;           // ✅ প্রোগ্রাম সফলভাবে শেষ
}


/* =====================================================
📥 Input:
(কোনো ইনপুট লাগে না, কারণ i=1 থেকে শুরু করা হয়েছে)

📤 Output:
2
4
6
...
98
100

🧠 Short Note:
এই প্রোগ্রামটি recursion ব্যবহার করে 1 থেকে 100 এর মধ্যে সব even সংখ্যা প্রিন্ট করে।
প্রতিবার 1 করে বাড়িয়ে চেক করে সংখ্যাটি even কিনা, হলে প্রিন্ট করে এবং ফাংশন আবার কল করে।

⏱️ Time Complexity (TC):  O(n)     // n = 100 → 100 বার ফাংশন কল হবে
💾 Space Complexity (SC): O(n)     // Recursion call stack-এ 100 টা ফ্রেম জমা হয়
===================================================== */
