#include <stdio.h>  // ইনপুট-আউটপুট ফাংশনের জন্য হেডার ফাইল

int main() { 
   int n, x;                 // n = কতগুলো সংখ্যা ইনপুট নিবে, x = প্রতিটি সংখ্যা ধরে রাখবে
   scanf("%d", &n);          // ইউজার থেকে সংখ্যা n ইনপুট নিচ্ছে

   for(int i = 0; i < n; i++){   // n বার লুপ চালাবে (প্রতিটি সংখ্যার জন্য একবার করে)
      scanf("%d", &x);           // প্রতিটি সংখ্যার ইনপুট নিচ্ছে

      while (x != 0) {           // যতক্ষণ পর্যন্ত সংখ্যা 0 না হচ্ছে
         printf("%d ", x % 10);  // সংখ্যাটির শেষ ডিজিট প্রিন্ট করছে
         x = x / 10;             // সংখ্যাটি ১০ দিয়ে ভাগ করে শেষ ডিজিট বাদ দিচ্ছে
      }

      printf("\n");             // প্রতিটি সংখ্যার জন্য এক লাইন
   }

   return 0;                    // প্রোগ্রাম সফলভাবে শেষ
}

/*
Input:
3
123
456
1000

Output:
3 2 1 
6 5 4 
0 0 0 1 

Note: প্রোগ্রামটি `n` সংখ্যক পূর্ণসংখ্যা ইনপুট নেয় এবং প্রতিটি সংখ্যার ডিজিটগুলো উল্টো করে (reverse order-এ) প্রিন্ট করে। যেমন, 123 দিলে প্রিন্ট হবে 3 2 1 💡
*/
