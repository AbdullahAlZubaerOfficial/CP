#include <stdio.h>  // Standard input-output লাইব্রেরি

int main() { 
     
    int n;  // কতগুলো স্ট্রিং ইনপুট নেবে তার সংখ্যা
    scanf("%d",&n);  // n ইনপুট নেওয়া

    while (n--)  // n বার লুপ চালানো
    {
        char s[101];  // প্রতিটি স্ট্রিং ধারণের জন্য array

        scanf("%s",s);  // স্ট্রিং ইনপুট নেওয়া

        for(int i=0; s[i]!='\0'; i++){  // স্ট্রিং এর প্রতিটি ক্যারেক্টারে লুপ
            printf("%c",s[i]);  // ক্যারেক্টার প্রিন্ট করা
           
            if(s[i+1]!='\0'){   // যদি এই ক্যারেক্টারের পর আরেকটি থাকে (শেষ ক্যারেক্টার না হয়)
                // তাহলে স্পেস প্রিন্ট করো
                
                // 🧠 উদাহরণ দিয়ে বুঝি:
                // ধরো ইনপুট দিলা: "123"
                // s[0] = '1'
                // s[1] = '2'
                // s[2] = '3'
                // s[3] = '\0'
                // লুপ চলবে:
                // i = 0 → print 1 → s[1] আছে → print space ✅
                // i = 1 → print 2 → s[2] আছে → print space ✅
                // i = 2 → print 3 → s[3] নেই → no space ❌
                // done.
                // Output: 1 2 3

                printf(" ");
            }
        }

        printf("\n");  // প্রতিটি স্ট্রিং এর পরে নতুন লাইন

    }
   
    return 0;
}

/*

Input: 
3
123
1245
436784

Output: 
1 2 3
1 2 4 5
4 3 6 7 8 4

Short Note:
এই প্রোগ্রামটি n টি স্ট্রিং ইনপুট নেয় এবং প্রতিটি স্ট্রিং এর ক্যারেক্টার গুলোর মধ্যে একটি করে স্পেস যোগ করে আউটপুট দেয়। 
শেষ ক্যারেক্টারের পরে স্পেস দেয়া হয় না।

Time Complexity: O(n * m)
যেখানে n = স্ট্রিং এর সংখ্যা এবং m = প্রতিটি স্ট্রিং এর দৈর্ঘ্য

Space Complexity: O(m)
স্ট্রিং সংরক্ষণের জন্য

*/
